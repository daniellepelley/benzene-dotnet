# Capability Matrix — what Benzene does, deliberately doesn't, and how to fill the gap

Benzene is honest about its boundaries. This page is the single place that states, for each
common production concern, **what Benzene provides**, **what it deliberately does not do (and
why)**, and **how to solve the rest outside Benzene**. Nothing here is a hidden gap — these are
design decisions that follow directly from Benzene's philosophy.

## The one idea behind every row

Benzene abstracts at the **business-logic boundary** — you write a message handler once and host
it anywhere — and **never at the transport or storage boundary**. This is the opposite of
frameworks like Dapr that hide the third party behind a generic interface (a "queue", a "state
store") and, in doing so, hide that third party's best features. If you wrap SQS behind a generic
queue interface, you lose the SQS-specific capabilities that were the reason to choose SQS.

So Benzene's answer to "does it abstract X?" is usually a deliberate **no** — you keep full,
direct access to the underlying SDK and all its cloud-native capabilities, and Benzene stays out
of the way. When Benzene doesn't ship an adapter for something, **rolling your own is a
first-class, supported path**: a small piece of middleware or a custom pipe into the pipeline, not
an escape hatch.

Two corollaries you'll see below:

- **A database is not a transport.** Benzene delivers events *in* (an ingress adapter), but it
  does not do database access. Persisting state is your handler's own code (`CosmosClient`,
  EF Core, Dapper, the AWS SDK, …).
- **Some problems can't be solved at runtime inside Benzene at all.** Benzene instances are
  independent processes (separate Lambda invocations, separate containers) that don't know about
  each other. Anything requiring cross-instance coordination (distributed idempotency,
  exactly-once) needs external shared state with atomic semantics — and even then, races are
  inherent. Benzene won't pretend otherwise.

## The matrix

| Capability | What Benzene provides | What it deliberately does NOT do (and why) | How to solve the rest |
|---|---|---|---|
| **Transport features** | Full, direct access to the underlying SDK message/context on every adapter | Hide transport-specific capabilities behind a generic interface (the anti-pattern above) | Use the raw SDK feature directly — the context exposes the native message/event |
| **Message routing** | Topic-based dispatch to `[Message]` handlers; the same handler runs behind every transport | Impose a canonical message envelope on transports that already carry routing (Kafka topic, Service Bus properties) | For envelope-less transports (Queue Storage, Event Hubs bodies) use the Benzene envelope or a preset topic; otherwise route on the native key |
| **Idempotency** | `IIdempotencyStore` seam + `InMemoryIdempotencyStore` (single-process) + atomic-claim middleware. Every claim is **fenced**: a win returns an opaque `ClaimToken` that `CompleteAsync`/`ReleaseAsync` require back, so a settle whose token no longer matches the live claim (reclaimed by another worker, or already settled) is refused (`false`, nothing written) rather than clobbering the current holder. `IdempotencyMiddleware` forwards the ambient `CancellationToken` (via `ICancellationTokenAccessor`) into every store call, matching the sibling `Benzene.ClaimCheck` middleware, so a wrapping `UseTimeout`/shutdown actually bounds a slow store call. **Fencing is token match alone** across every implementation (`InMemoryIdempotencyStore`, `DynamoDbIdempotencyStore`, both Outbox stores) — a holder that merely outraces its own TTL, with nobody having reclaimed the key, can still settle with its original token. `DynamoDbIdempotencyStore.TryClaimAsync` never reports a win it didn't write: a conditional `PutItem` that loses to a live record which then reads back absent (a race with a concurrent release) is bounded-retried rather than synthesized as a win — every `Won` corresponds to an actual successful write. | Cross-instance de-duplication — independent processes can't coordinate at runtime without external state, and adding shared state can just relocate the race, not remove it | An external store with an **atomic conditional write** (DynamoDB conditional put, Redis `SET NX`) keyed on message identity, **plus** handlers designed to be naturally idempotent. See [Idempotency](cookbooks/idempotency.md). |
| **Outbox (produce-side durability/atomicity)** | `UseOutbox()` route middleware (`Benzene.Outbox`) with two write modes: **`Immediate`** (default) durably stores-and-forwards the send — survives process death/transport outages, retried with backoff, never silently swallowed, but **not** atomic with the handler's own state write; **`Transactional`** stages the envelope and defers the write to a store's own unit of work. Delivery is always **at-least-once**, never exactly-once, and has **no ordering guarantee** across envelopes. Every claim is **fenced**: `ClaimDueAsync`/`ClaimAsync` stamp a `LeaseToken` that `MarkDispatchedAsync`/`RescheduleAsync`/`ParkAsync` require back, closing the live-but-slow-claimant hole (a lapsed lease reclaimed by another worker can no longer have its settle clobbered by the original, stale claimant) — it does not and cannot recall a message a stale claimant already handed to the transport before its lease lapsed. | Make `Transactional` mode's atomic commit real on its own — that needs a store package. Exactly-once delivery, or any ordering guarantee across envelopes (both explicitly out of scope, not just unbuilt). Crash-after-send remains an inherent at-least-once window — fencing narrows it, it does not close it. | For the atomic commit, add `Benzene.Outbox.DynamoDb` (one `TransactWriteItems` via `IDynamoDbOutboxTransaction`) or `Benzene.Outbox.EntityFramework` (same-`DbContext` change tracker + your own `SaveChangesAsync`). At-least-once delivery means consumers **must** dedup — pair every outboxed route with [Idempotency](cookbooks/idempotency.md) on the receiving end (the outbox stamps an `idempotency-key` header by default so the two packages click together with zero extra config). See [Transactional Outbox](cookbooks/transactional-outbox.md). |
| **Resilience** | `UseRetry` — retry with exponential backoff, an optional max-delay cap, and pluggable jitter (`RetryMiddleware.FullJitter()`); `UseTimeout` — a deadline that becomes a `timeout` failure result while a genuine host cancellation still propagates, correctly classified even under nested `UseTimeout` composition regardless of which layer's timer actually fired; or the full Polly toolkit (circuit breaker, hedging, fallback, rate limiting, richer timeout policies) via `Benzene.Resilience.Polly`'s `.UseResiliencePipeline(...)`, including a `.ExcludingCancellation()` `PredicateBuilder` helper so widening a strategy's `ShouldHandle` (e.g. a circuit breaker) doesn't silently drop Polly's own cancellation-safe default | Benzene does not re-implement or hide Polly behind its own abstraction — the Polly package runs *your* `ResiliencePipeline`, exposing its full configuration surface | For retry/timeout with zero extra dependency use `UseRetry`/`UseTimeout`; for anything more add `Benzene.Resilience.Polly` and drop a Polly `ResiliencePipeline` into `.UseResiliencePipeline(...)` — it even bridges a returned failure result to Polly's outcome model. See [Polly Resilience Pipelines](cookbooks/polly-resilience.md) and [Resilience](resilience.md). |
| **Oversized payloads (transport size limits)** | `Benzene.ClaimCheck` — an optional offload/hydrate middleware pair: `UseClaimCheck()` offloads a payload over a configurable threshold to a pluggable `IClaimCheckStore` and carries a reference on the `benzene-claim-check` header; `UseClaimCheck<TContext>()` resolves it back before deserialization, forwarding the ambient `CancellationToken` (via `ICancellationTokenAccessor`) into every store call so a wrapping `UseTimeout`/shutdown actually bounds it. Store packages: `Benzene.ClaimCheck.Aws.S3`, `Benzene.ClaimCheck.Azure.Blob`. The built-in `InMemoryClaimCheckStore` reclaims an expired entry when it is next read, and separately sweeps all expired entries on `PutAsync` (at most once per minute) so a payload that is never read back at all still gets reclaimed, without a background thread. | Compression (a smaller payload avoiding offload entirely), delete-on-consume (fan-out and at-least-once redelivery make it unsafe — see the doc), and interop with a third party's own offload wire format (e.g. the SQS Extended Client Library rides its reference in the body, not a header, so it isn't a drop-in swap) | Retention is TTL-based, sized to exceed queue retention plus any DLQ redrive window (an infrastructure lifecycle rule, not created by the package). See [Claim Check](claim-check.md). |
| **Sagas / workflows** | In-process, compensation-based saga with LIFO rollback (`Benzene.Saga`). A built `Saga` (`SagaBuilder.Build()`) is an immutable descriptor tree — no per-execution outcome is ever stored on its stages/steps — so the same built `Saga` instance is safe to call `RunAsync()` on concurrently, any number of times, without one run's outcome corrupting another's. | Durable crash-resume — the saga is in-memory `Func` closures that can't be re-hydrated after a process dies; the state store records progress for *observability*, not recovery | For crash-durable, long-running or human-in-the-loop workflows, use a real durable orchestrator: AWS Step Functions, Azure Durable Functions, Temporal, or similar. Benzene's `Benzene.Clients.Aws.StepFunctions` package can *start* a Step Functions execution from a handler, but running/resuming the workflow itself is the orchestrator's job, not Benzene's. See [Sagas](cookbooks/sagas.md). |
| **Schema evolution** | The Confluent wire-format codec (solid, tested) + an `ISchemaRegistryClient` seam | Structural Avro/JSON backward-compatibility checking in-box — the shipped `TextualSchemaCompatibilityChecker` only accepts byte-identical schemas. `SchemaRegistrySerializer.Deserialize` also does not validate the framed schema id against the caller's requested `Type` — it's producer-side interop framing, not registry-driven consumer resolution, so a corrupted/misrouted payload deserializes as whatever type the caller asked for rather than being rejected | Point at a real schema-registry server, or supply your own `ISchemaCompatibilityChecker`. See [Schema Registry](cookbooks/schema-registry.md). |
| **Database / state access** | *(nothing — by design)* | Any database or state-store abstraction — a database is not a transport, so wrapping one would hide its capabilities (the core anti-pattern) | Your handler uses its own SDK/ORM directly (`CosmosClient`, EF Core, Dapper, `DynamoDBContext`, …). Benzene delivers the event in; persistence is yours |
| **Configuration & secrets** | Provider-agnostic `ISecretStore` (env vars, mounted files, composed, cached) + fail-fast startup validation | Ship maintained cloud secret-store adapters (Key Vault / Secrets Manager / SSM) as packages | Copy the ~30-line adapter from [Secrets & Configuration](cookbooks/secrets-configuration.md) and reference the cloud SDK yourself (a shipped adapter is a candidate for post-1.0) |
| **Outbound HTTP** | `Benzene.Clients.Http` middleware over an `HttpClient` you supply | Manage `HttpClient` lifetime for you (no built-in `IHttpClientFactory` wiring on the low-level path) | Register a typed/`IHttpClientFactory` client and pass it in; correlation/trace propagation is applied on the Benzene-message outbound path |
| **Distributed tracing** | W3C `traceparent`/`tracestate` inbound extraction on HTTP **and** async transports (SQS, SNS, Kafka — AWS Lambda, Azure Functions, and the self-hosted worker — and Event Hub, Azure Functions and the self-hosted worker); OpenTelemetry, exporter-agnostic. The root `"W3CTraceContext.Root"` span and the per-stage handler span are both marked `Error` (with the exception attached) when downstream code throws, so a failed request is never indistinguishable from a successful one in a trace viewer. `ICorrelationId`/`InboundCorrelationIdMiddleware` bound and sanitize a caller-supplied correlation-id header (length-capped, control characters incl. `\r`/`\n` rejected) before it reaches a log scope or an outbound header, falling back to the self-generated id on rejection. | Sampling/backend-specific configuration (Jaeger, Application Insights, etc.) is yours to wire — Benzene exports via the standard OTel API, it doesn't ship a backend | `.UseW3CTraceContext()` as the first middleware on any pipeline continues a caller's trace instead of starting a new one per hop. See [Monitoring & Diagnostics](monitoring.md) and the [distributed tracing cookbook](cookbooks/distributed-tracing-opentelemetry.md). |
| **Retry-on-handler-failure-result** | Per-transport: some retry a returned `IBenzeneResult` failure (`IsSuccessful == false`) the same as an exception by default; some only retry it if you opt in; some can't retry it at all today. See the breakdown below — **the default is not uniform across transports, and on several it's silently unsafe.** | A single cross-transport reliability abstraction — retry semantics are inherently transport-native (Lambda batch-item-failure vs. Service Bus abandon vs. EventBridge destinations), so Benzene surfaces each transport's own mechanism rather than inventing one | Know your transport's default (below); opt in where an `Options`/`AckMode` knob exists; if none exists, have the handler throw instead of returning a failure result for anything that must be retried. **Any handler that can be retried needs to be idempotent** — see the Idempotency row above. |
| **Multi-tenancy** | *(nothing as a framework feature today)* | — (a candidate for post-1.0) | Roll a tenant-resolver middleware + a scoped tenant holder (the documented [Multi-Tenancy](cookbooks/multi-tenancy.md) pattern) |
| **AuthN / AuthZ** | OAuth2 bearer (JWT) validation with a strict algorithm allowlist + scope-based authorization (`Benzene.Auth.OAuth2`); signing keys are resolved via full OIDC discovery (`Authority`) or a bare JWKS document (`JwksUri`), both cached with refresh-on-unrecognized-`kid` (`OAuth2ConfigurationManagerFactory`/`JwksOnlyConfigurationRetriever`). `OAuth2BearerOptions.Validate()` fails fast at wire-up (not on the first request) on: a non-`https` `Authority`/`JwksUri` unless `RequireHttpsMetadata` is explicitly `false`; a null/whitespace/`"*"` entry in `ValidIssuers`/`ValidAudiences`; a `ValidAlgorithms` entry that is `"none"` or not one of the standard JWS signing algorithms (`Microsoft.IdentityModel.Tokens.SecurityAlgorithms`'s HS/RS/ES/PS 256/384/512 constants); and a `ClockSkew` above `OAuth2BearerOptions.MaxClockSkew` (15 minutes). `Benzene.Auth.Core`'s role/policy authorization (`RequireRole`/`RequirePolicy`/`RequireAuthorization`) reads roles via a single `RoleClaims` helper (`ClaimTypes.Role`/`role`/`roles`, the last also expanding a JSON-array value — Azure AD's app-roles shape) that `deploy/Mesh/Benzene.Mesh.Host`'s `MeshAuthGate` shares rather than reimplementing, so both agree on what counts as a role | Ship a policy-engine (OPA/Cedar) adapter, or rate-limiting middleware | Add an authorization middleware calling your policy engine; the `Benzene.Auth.Core` seams are there to build on |

### Retry-on-handler-failure-result — the per-transport breakdown

"Failure result" below means your handler returned `IsSuccessful == false` (e.g.
`BenzeneResult.ServiceUnavailable(...)`) — **not** a thrown exception. Every transport in Benzene
retries an unhandled *exception* by default (that's what makes it propagate to the host's own
retry machinery); what varies is what happens to a *returned* failure result, which is the
surprising case because nothing crashed.

| Transport | Default on a returned failure result | Opt-in fix |
|---|---|---|
| AWS Lambda SQS (`Benzene.Aws.Lambda.Sqs`) | **Safe** — retried per-message via `ReportBatchItemFailures`, same as an exception | N/A (default is already safe); `SqsOptions.BatchFailureMode = FailWholeBatch` to change the *shape* of the retry, not to enable it |
| AWS DynamoDB Streams (`Benzene.Aws.Lambda.DynamoDb`) | **Safe** — sequential processing stops at the first failed record and reports it for redelivery | N/A |
| AWS Kinesis (`Benzene.Aws.Lambda.Kinesis`) | **Safe by design** — the stream checkpoints only where your handler explicitly checkpoints; an unfailed-but-uncheckpointed record is redelivered | N/A — see [Benzene.Aws.Lambda.Kinesis's CLAUDE.md](../src/Benzene.Aws.Lambda.Kinesis/CLAUDE.md) |
| AWS SNS (`Benzene.Aws.Lambda.Sns`) | **Safe by default** — `SnsOptions.RaiseOnFailureStatus` defaults `true`, escalating a failure result into a throw so SNS's subscription retry/redrive applies | `SnsOptions.RaiseOnFailureStatus = false` for at-most-once — see [SNS Fan-Out Pattern](cookbooks/sns-fan-out.md) |
| AWS EventBridge (`Benzene.Aws.Lambda.EventBridge`) | **Safe by default** — `EventBridgeOptions.RaiseOnFailureStatus` defaults `true`, escalating a failure result into a throw so EventBridge's retry/DLQ applies | `EventBridgeOptions.RaiseOnFailureStatus = false` for at-most-once |
| AWS Kafka/MSK (`Benzene.Aws.Lambda.Kafka`) | **Safe by default** — `KafkaOptions.BatchFailureMode` defaults to `PartialBatchFailure`, reporting each failed partition's resume offset via the batch response (needs `ReportBatchItemFailures` on the event source mapping, as with SQS) so records are redriven from the failure, honouring per-partition ordering | `KafkaOptions.BatchFailureMode = FailWholeBatch` (throws) when the event source mapping isn't configured for `ReportBatchItemFailures` |
| AWS S3 (`Benzene.Aws.Lambda.S3`) | **Safe by default** — `S3Options.RaiseOnFailureStatus` defaults `true`, escalating a failure result into a throw so the Lambda invocation fails and the event's async retry/DLQ applies | `S3Options.RaiseOnFailureStatus = false` for at-most-once |
| AWS SQS self-hosted worker (`Benzene.Aws.Sqs`'s `SqsConsumer`) | **Safe by default** — `SqsConsumerOptions.AckMode` defaults to `SqsConsumerAckMode.PerMessage`, so only successfully-handled messages are deleted and a failure result is left for redelivery | `SqsConsumerAckMode.WholeBatch` for at-most-once (delete the whole batch regardless) |
| RabbitMQ self-hosted worker (`Benzene.RabbitMq`) | **Safe by default** (`RabbitMqAckMode.Explicit`) — a failure result (or exception) nacks the message for requeue/dead-letter, not ack | N/A (default is already safe); `RequeueOnFailure`/`AckMode` tune *how* it's redelivered, or `AckMode = AutoAck` to opt into at-most-once |
| Azure Service Bus Function trigger (`Benzene.Azure.Function.ServiceBus`) | **Safe by default** — although `AckMode` defaults to `AutoComplete`, `ServiceBusOptions.RaiseOnFailureStatus` defaults `true`, so a failure result throws (→ the message is abandoned, not completed). Under `AckMode = Explicit`, `state.Acked` is set only *after* the `CompleteMessageAsync`/`AbandonMessageAsync` settle call itself returns successfully (`ServiceBusApplication.OnPipelineSucceededAsync`) — a throwing settle call is exactly the case the base class's exception-recovery fallback-abandon exists for, and setting `Acked` up front used to skip it | `RaiseOnFailureStatus = false` for at-most-once; `AckMode = Explicit` for true per-message complete/abandon control |
| Azure Service Bus self-hosted worker (`Benzene.Azure.ServiceBus`) | **Safe by default** — `BenzeneServiceBusConfig.AckMode` defaults to `ServiceBusConsumerAckMode.Explicit`, so a failure result (or throw) abandons the message for redelivery | `AckMode = ServiceBusConsumerAckMode.AutoComplete` for at-most-once |
| Azure Kafka/Event Hubs Function trigger (`Benzene.Azure.Function.Kafka`) | **Safe by default** — `KafkaOptions.RaiseOnFailureStatus` defaults `true`, escalating a failure result into a throw so the trigger re-delivers | `KafkaOptions.RaiseOnFailureStatus = false` for at-most-once |
| Azure Event Grid (`Benzene.Azure.Function.EventGrid`) | **Safe by default** — `EventGridOptions.RaiseOnFailureStatus` defaults `true`, escalating a failure result into a throw so Event Grid's retry/dead-letter (backoff up to 24h) applies | `EventGridOptions.RaiseOnFailureStatus = false` for at-most-once |
| Azure Event Hubs Function trigger (`Benzene.Azure.Function.EventHub`) | **Safe by default** — `EventHubOptions.RaiseOnFailureStatus` defaults `true`, escalating a failure result (on both the property-based and envelope routing paths) into a throw so the trigger re-delivers the batch | `EventHubOptions.RaiseOnFailureStatus = false` for at-most-once |
| Azure Queue Storage (`Benzene.Azure.Function.QueueStorage`) | **Safe by default** — `QueueStorageOptions.RaiseOnFailureStatus` defaults `true`, escalating a failure result (on both the preset-topic and envelope routing paths) into a throw so the host's `maxDequeueCount` retry/poison handling applies | `QueueStorageOptions.RaiseOnFailureStatus = false` for at-most-once |
| Google Cloud Pub/Sub (`Benzene.GoogleCloud.Functions.PubSub`) | **Safe by default** — `PubSubOptions.RaiseOnFailureStatus` defaults `true`, escalating a failure result into a throw so Pub/Sub redelivers per the subscription's ack-deadline/retry policy | `PubSubOptions.RaiseOnFailureStatus = false` for at-most-once |
| Kafka self-hosted worker (`Benzene.Kafka.Core`) | **At-most-once by default** — a stream worker (see the callout below) can't redeliver one record without halting, so `BenzeneKafkaConfig.CommitOnlyOnSuccess` defaults `false`: offsets auto-commit regardless of outcome, so a failed record is skipped on restart (a returned failure result is at least logged — `RaiseOnFailureStatus` defaults `true`) | `BenzeneKafkaConfig.CommitOnlyOnSuccess = true` (requires `CatchHandlerExceptions = false`) for at-least-once — offsets are then stored only after a successful handle, so a restart redelivers the failed record; or wire a `DeadLetterTopic`. Under either, `RaiseOnFailureStatus` (default `true`) settles a *returned* failure result the same way as a thrown one; set it `false` to accept a failure result as settled |
| Azure Event Hubs self-hosted worker (`Benzene.Azure.EventHub`) | **At-most-once by default** — same stream constraint: `BenzeneEventHubConfig.CatchHandlerExceptions` defaults `true`, so a failed event (or an escalated failure result — `RaiseOnFailureStatus` also defaults `true`) is logged and skipped once a later event checkpoints past it | `BenzeneEventHubConfig.CatchHandlerExceptions = false` for at-least-once — the worker then stops at the failure without checkpointing, so a restart redelivers (and `RaiseOnFailureStatus = false` additionally accepts a failure result as settled) |
| Azure Cosmos DB Change Feed — Functions trigger (`Benzene.Azure.Function.CosmosDb`) | N/A as a "failure result" concern — like Kinesis, this is a fan-in `StreamContext<TDocument>` with no `IBenzeneResult`/message-handler routing; the trigger's lease checkpoints the whole batch on any non-throwing return, and unlike Kinesis there's no per-document checkpoint API to opt out of that with (see the package's own `CLAUDE.md`). The Blob trigger (`Benzene.Azure.Function.BlobStorage`) is the other deliberately non-routed Azure adapter — it delivers a blob's name + content to `UseBlob(...)` rather than routing a message, with the same throw-to-redeliver escape hatch | Have the handler throw for anything that must redeliver the whole batch |
| Azure Cosmos DB Change Feed — self-hosted worker (`Benzene.Azure.CosmosDb`) | N/A as a "failure result" concern, same fan-in `StreamContext<TDocument>` shape as the Functions trigger above, but with manual checkpoint control the trigger doesn't expose. `BenzeneCosmosChangeFeedWorker`'s `BenzeneCosmosChangeFeedConfig.AutoCheckpointOnSuccess` defaults `true` (checkpoints automatically once the pipeline completes, unless the handler already checkpointed itself via the batch's own checkpointer) and `CatchHandlerExceptions` defaults `false` (a failed batch propagates uncheckpointed, so the SDK's Change Feed Processor redelivers it — at-least-once by default, the opposite default from the Event Hubs self-hosted worker above, because the change feed natively retries a failed batch and Event Hubs doesn't); its sibling `BenzeneCosmosAllVersionsChangeFeedWorker` (all-versions-and-deletes mode) is automatic-checkpoint-only — no `AutoCheckpointOnSuccess` knob exists, only `BenzeneCosmosAllVersionsChangeFeedConfig.CatchHandlerExceptions` (same default/semantics). On `BenzeneCosmosChangeFeedWorker` only, the auto-checkpoint call now runs *after* and *outside* the handler's own try/catch (round-10 #108): a lease-container write failure following a successfully-processed batch is logged as a checkpoint failure (naming the lease token, not the handler) and left un-checkpointed for redelivery, never misattributed to the handler and never retried a second time with zero backoff | `CatchHandlerExceptions = true` on either worker for skip-and-continue (permanently passes over a poison batch); `AutoCheckpointOnSuccess = false` on `BenzeneCosmosChangeFeedWorker` for fully manual checkpoint control via the batch's own checkpointer |
| HTTP / API Gateway | N/A — a failure result maps straight to an HTTP status code the caller sees synchronously; there's no async "retry" concept to opt into | N/A |

**The 1.0 settlement contract** (maintainer-approved 2026-07-21): every **queue-shaped** transport is
**safe by default** — a handler that *returns* a failure `IBenzeneResult` (rather than throwing) is
**not settled by default**; it is escalated as that transport's `*MessageProcessingException` so the
platform redelivers it (at-least-once), instead of being silently acked/completed/checkpointed past
(at-most-once). Mechanically, `RaiseOnFailureStatus` defaults `true` on every queue-shaped options
type (AWS Lambda SNS/S3/EventBridge; the Azure Functions Kafka, Queue Storage, Event Grid, Event Hub
and Service Bus triggers; the self-hosted Event Hub worker; Google Cloud Pub/Sub), while
`Benzene.Aws.Sqs` (`AckMode = PerMessage`), `Benzene.Azure.ServiceBus` and `Benzene.RabbitMq`
(`AckMode = Explicit`) were already safe, and Kinesis / DynamoDB Streams settle via
`ReportBatchItemFailures` instead. Each transport exposes an explicit opt-out (the "Opt-in fix" column
shows the reverse) for the at-most-once cases where you deliberately want a returned failure accepted.
`SettlementContractDefaultsTest` guards these defaults against drift; the migration record behind this
contract is archived at `work/archive/settlement-contract-1.0-2026-07.md`.

**The two self-hosted *stream* workers are the deliberate exception — they default to at-most-once.**
A stream (a Kafka partition, an Event Hub partition) has no per-message ack/abandon: the only way to
*not* lose a failed record is to stop the worker and never advance the offset/checkpoint past it. That
is far too drastic a default (one poison record would halt all processing), so `Benzene.Kafka.Core`
(`CommitOnlyOnSuccess = false`) and `Benzene.Azure.EventHub` (`CatchHandlerExceptions = true`) default
to **skip-and-continue** — a failed record is logged and the stream advances past it. Opt into
at-least-once with `CommitOnlyOnSuccess = true` / `CatchHandlerExceptions = false` respectively, and
accept that a poison record then wedges/halts the worker until it's handled or dead-lettered. This is
a genuine property of streams, not an oversight — the **Lambda/Functions** stream *triggers* (Kinesis,
DynamoDB Streams, the Kafka/Event Hub Function triggers) can be safe-by-default because the platform
re-invokes them from the un-advanced checkpoint without any single process having to halt; a
long-running self-hosted worker has no such external re-invoker.

The rows marked N/A above (Cosmos DB Change Feed, HTTP) have no per-item async-retry knob to opt into
— there, a handler that `throw`s is the escape hatch for anything that must redeliver.

## Area rows — packages and surfaces

The rows above are organised by production concern; the rows below are organised by **package or
area**, in the shape the sibling ports' matrices use, so the cross-port matrix can read this port
row-for-row. Same rules: every "provides" claim names the code that provides it, a deliberate "no"
states its reason, and anything simply unbuilt says "not implemented" plainly.

| Area | What it provides | What it deliberately does NOT do (and why) | How to solve the rest |
|---|---|---|---|
| **gRPC** | `Benzene.Grpc` — routes all four RPC shapes (unary, server-streaming, client-streaming, bidirectional; `BenzeneInterceptor` overrides each) into message handlers via `[GrpcMethod]`, with protobuf⇄JSON bridging and status-code mapping; `Benzene.Grpc.AspNet` — ASP.NET Core hosting with opt-in grpc.health.v1 (including named liveness/readiness services, each an aggregate `BenzeneHealthCheckBridge` that fails fast at construction — not at first probe — if a configured `LivenessCheckTypes`/`ReadinessCheckTypes` entry matches no registered check, and suffixes duplicate `Type` keys in its reported data rather than letting one collide over another) and reflection; `Benzene.Grpc.Client` — outbound unary client with inbound-deadline propagation, classifying ambient send-time cancellation (`OperationCanceledException`) as a `ServiceUnavailable` result without Error-level logging, same as a mid-flight `RpcException(Cancelled)`; `Benzene.Grpc.Versioning` — request-side payload-version casting; `Benzene.Grpc.TestHelpers` | Own the gRPC server or generate service stubs — the interceptor claims only routed methods; unmatched calls fall through untouched to the native service implementation. Response-side payload downcasting over gRPC — the result setter writes protobuf directly, with no response payload mapper to decorate (see `Benzene.Grpc.Versioning`'s CLAUDE.md) | Register real protoc-generated services alongside; model an older *response* shape as a distinct method/topic |
| **Mesh — service side** | `Benzene.Mesh.Wire` — the `mesh.md` wire layer: `ServiceDescriptor` derived from the live handler registry (topics + `consumes` via `MeshOutboundRegistry` + payload schemas + `descriptorHash`), reserved `mesh` topic middleware, trace middleware with W3C traceparent join + the batching HTTP trace exporter, and the deduplicating issue emitter (`IMeshIssueExporter`); `Benzene.Mesh.Reporting` — push self-report for services nothing can poll (SQS-only Lambdas etc.) | Turn any feed on by default — every mesh feed is opt-in and independent, and a service is fully functional without the mesh | `UseBenzeneCloudService` (row below) wires descriptor, trace feed and collector registration in one call |
| **Mesh — collector** | `Benzene.Mesh.Collector` — `benzene:mesh:register`/`heartbeat`/`traces`/`issues` ingest plus `benzene:mesh:query:{fleet,service,topic,trace,correlation}` read models over the in-memory `MeshCollectorStore` (bounded trace ring; the producer/consumer graph is declared by descriptors, never trace-derived); `CompositeMeshFleetReadModel` composes tracing/usage backends instead of the store (`Benzene.Mesh.Fleet.Tempo`/`.Jaeger`/`.Aws.XRay`, `Benzene.Mesh.Tracing.Tempo`, `Benzene.Mesh.Usage.CloudWatch`/`.ApplicationInsights`); the pull-based catalog pipeline — `Benzene.Mesh.Aggregator` + `Benzene.Mesh.Contracts` + `Benzene.Mesh.Artifacts`, discovery via `Benzene.Mesh.Discovery.Aws`/`.Azure`/`.Kubernetes`, artifact stores `Benzene.Mesh.Aws.S3`/`.Azure.Blob`/`.GoogleCloud.Storage`, with failure isolation at every fan-out in the pipeline — one failing `IMeshDiscoveryProvider` (each individually timed and try/caught, `MeshDiscoveryRunner.PerProviderTimeout`), one failing per-function AWS Lambda tag read, or one throttled previous-snapshot read never costs the rest of the run its results, and `Benzene.Mesh.Discovery.Host` still refuses to publish outright only when *every* configured provider failed (a partial-provider failure still publishes, with the failures surfaced); `MeshAggregator.RunOnceAsync` itself now holds the single-writer gate (every call site serialises through it, not just hosts that remembered their own), stamps every artifact of one run with the same timestamp as a de facto run id, and publishes `manifest.json` last so a reader never sees it reference an artifact that hasn't landed; a spec/health fetch failure additionally classifies as permission/unreachable/timeout/other (`MeshServiceSnapshot.ErrorClass`) via each SDK's own status-code shape, read without a compile-time dependency on any of them; `KubernetesApiServiceLister` paginates (`limit`+`continue`) rather than silently truncating at the API server's first page; `FileSystemMeshArtifactStore` writes are atomic (temp file + rename), so a concurrent read of the same file it's writing never observes a torn value; a topic's request/response/message schema is diffed version-over-version via `JsonSchemaComparer` into the published catalog's `MeshTopicCompatibility` verdict (breaking/warning/compatible), correctly seeing through `InlineSchema`'s pre-comparison `$ref` inlining so a pure `oneOf`/`anyOf`/`allOf` branch reordering reads as compatible rather than a fabricated breaking change (round 11, #152); opt-in production-gated live dispatch (`Benzene.Mesh.Dispatch`, one declared service, never a shared queue); OIDC login gate for the whole mesh surface (`Benzene.Mesh.Auth.Oidc`, Authorization Code flow + email allowlist, a `Validate()`-time fail-fast on a non-https issuer matching `MeshAuthGate`'s, `POST /mesh/auth/logout` requiring the `X-Benzene-Logout` header (`405` on GET), a per-scope `IOidcSessionSink` identity handoff, and `MeshOidcOptions.Validate()` also rejecting a low-entropy `SigningKey` — fewer than 8 distinct byte values across its length — since that one key signs both the CSRF state token and the deterministic, stateless session cookie); `deploy/Mesh/Benzene.Mesh.Host` — the config-driven, dockerized aggregator+UI deployable composing all of the above from `mesh.json`, gated by its own `MeshAuthGate` (`none`/`proxy`/`basic`/`oidc`, `proxy` mode's trusted-peer check normalizing IPv4-mapped IPv6 addresses so it works on a dual-stack listener (e.g. Kestrel bound to `[::]`, the container default), with a startup-time mode×option satisfiability check — `requiredGroups`/`dispatchRole`/`allowedEmailDomains`/`dispatch.enabled` each rejected under whichever modes can't satisfy them, plus `dispatchRole` separately rejected when `dispatch.enabled` is `false` (the role check gates an endpoint that doesn't exist without it) — never silently under-enforced, never inert; `auth.oidc.requireHttpsMetadata` for the non-https-authority case; `POST /mesh/auth/logout` with IdP end-session redirect; `UseMeshDispatchGuard` bounds a dispatch request by its actual buffered byte count rather than a trustable-only-in-theory `Content-Length` header (chunked-transfer-proof), backed by Kestrel's own `MaxRequestBodySize` as defence-in-depth; `MeshArtifactMiddleware`/`MeshTimeRangeResolver` degrade a transient artifact-store failure or an out-of-range relative time query to a clean `503`/absent bound respectively, never an unhandled exception on the dashboard's read path — see `deploy/Mesh/CONFIG.md`) | Durable storage for the spec collector — it is a diagnostic surface, not a system of record; the durable output is the aggregator's *published catalog artifacts*, not collector state. Enforce an upper bound on `MeshOidcOptions.SessionDuration` — `Benzene.Mesh.Auth.Oidc` logout is stateless (no server-side session store, only a random `Jti` carried for a future deny-list that nothing reads yet), so a leaked/stolen session cookie stays valid until its own expiry regardless of the holder logging out; a deliberate, documented tradeoff (a hard cap could break a deployment's genuinely-needed longer "remember me" duration), not an oversight | Any port's collector can host this port's services — the `benzene:mesh:*` wire contract (pinned by the conformance fixtures) is the interop point |
| **Mesh — UI** | `Benzene.Mesh.Ui` — serves the **canonical** `mesh-ui.html` as an embedded resource (byte-identical to the main repo's vendored copy; the `mesh-ui-drift-check.yml` workflow fails CI if it drifts from the benzene-ui build), plus `mesh-spec-ui.html`, the per-service spec view the estate page links to | Render its own page — this port ships the shared cross-language UI; the `benzene:mesh:query:*` contract, not the page, is the interop point for any other UI | Point any UI at the collector's query topics |
| **Health checks** | `Benzene.HealthChecks.Core` (`IHealthCheck.ExecuteAsync(CancellationToken)` — the token is required, forwarded by every shipped check into its own I/O, so a per-check timeout genuinely cancels the underlying call instead of merely abandoning it; check/result/builder seams) + `Benzene.HealthChecks` reserved-topic middleware: `UseHealthCheck` (`healthcheck`), `UseLivenessCheck`/`UseReadinessCheck` (`liveness`/`readiness`, deliberately non-overlapping topics), `UseContractsCheck` (`contracts`), with built-in memory, timeout-wrapper and shutdown-readiness checks; dependency checks `Benzene.HealthChecks.Disk`/`.Http`/`.Tcp`/`.DynamoDb`/`.EntityFramework`/`.Schema`/`.Azure.ServiceBus`, plus the AWS/Azure client packages' own (`SqsHealthCheck`, `SnsHealthCheck`, `EventBridgeHealthCheck`, …), all relying purely on the processor's uniform timeout rather than an internal one; consumer-side downstream checks (`Benzene.Clients.HealthChecks` — reachability + contract drift); the ASP.NET Core/grpc.health.v1 bridge (`BenzeneHealthCheckBridge` in `Benzene.Grpc.AspNet`). Every shipped check reports a caller-driven cancellation (ambient shutdown, or the processor's own per-check timeout) as a distinct `"Cancelled"` outcome via `ExceptionHandlingHealthCheck`, never as an ordinary transient dependency failure — fixed once in `HealthCheckError.Classify` for the checks that route through it, plus an explicit `catch (OperationCanceledException) { throw; }` guard ahead of the catch-all in every check that instead builds its own failure result directly (`TcpHealthCheck`, `DynamoDbHealthCheck`, `HttpPingHealthCheck`, the two `Benzene.HealthChecks.EntityFramework` checks, `ClientHealthCheck`, `CacheHealthCheck`), plus a matching per-check guard in the two checks (`GrpcHealthCheck`, `RabbitMqHealthCheck`) that also enforce their own connect/round-trip budget on top of the caller's token, so *that* budget elapsing still correctly reports an ordinary Failed. `HttpPingHealthCheck` additionally reports `Url` + the dependency entry (not just an opaque exception) when the endpoint doesn't respond at all (`HttpRequestException`), the same as it already did for a non-2xx status — the failure mode where an operator running several ping checks most needs to know which endpoint is down. `HealthCheckProcessor` itself never lets one check's own broken `Timeout`/`IsNonCritical`/`Type` getter crash the whole aggregation — a throwing getter degrades to a `Failed` result for that one check, every other check's real result is unaffected. `CachingHealthCheckProcessor` single-flights concurrent callers per cache key (`ConcurrentDictionary<string, Lazy<Task<...>>>`), so a cold-cache (or just-expired) stampede runs the inner checks exactly once no matter how many callers arrive concurrently, rather than once per caller | Ship a dedicated `/health` HTTP route — health is a message topic; the HTTP transport maps the aggregated result to 200/503. Wire contract checks into probes — a drifting or struggling downstream must never restart otherwise-healthy pods, so `contracts` is a diagnostic topic, kept separate from liveness/readiness by design | Implement `IHealthCheck` for any other dependency; see [Kubernetes health checks](kubernetes-health-checks.md) |
| **Spec endpoint & Cloud Service Profile** | `Benzene.Schema.OpenApi` — the derived spec from registered handlers/endpoints, served on the `spec` topic in three formats (Benzene-native `EventService` document, OpenAPI 3.0, AsyncAPI 3.0); `SchemaBuilder`'s reflection-based generation (`JsonSerializerDataContractResolver`) camelCases property names to match the wire, the spec's own `example` block, and the sibling `.service.json` from the same build — all four now agree on casing (round 11, #169; previously the schema alone was PascalCase, self-contradicting the other three). Two ripples of that fix, closed in the same round: `OpenApiValidationSchemaBuilder` now camelCases a validation rule's own (reflected, PascalCase) member name before matching it against the now-camelCase schema property keys — left unfixed, every validation facet (`Required`, `MinLength`, `Pattern`, ...) would have silently stopped attaching to any schema at all; `JsonOpenApiSchemaBuilder` (the JSON-literal schema inferrer behind `AddJsonEvent`) capitalizes a nested object's inferred component id, since a schema id is conventionally a type name, not a (now camelCase) JSON property name; `Benzene.Spec.Ui` — the topic-centric spec viewer page; `Benzene.CloudService` — `UseBenzeneCloudService(...)`: one call wiring the profile's R1–R8 at the `/benzene/*` default paths (invoke envelope, spec, health, mesh descriptor, trace feed, collector registration + heartbeats), producing an honest wiring-time `CloudServiceProfileReport` stamped on the descriptor — R8 (trace context propagation) reads the exact same resolved trace-exporter value `Extensions.cs` wires `UseMeshTrace` with (round 11, #167; previously R8 reported satisfied whenever mesh was merely enabled, even with no collector/exporter configured and no trace middleware actually wired); `Benzene.CloudService.Probe` — external black-box R1–R8 probe over real HTTP against any running service, .NET or not; `Benzene.Descriptor` — the `benzene-descriptor` dotnet tool emitting `{name}.spec.json`/`{name}.service.json` from a built, non-running service (`--version-scheme` is validated at build time but does not yet reach the emitted descriptor — `MeshServiceInfo`/`MeshServiceDescriptor` carry no scheme field; tracked as a `[DECISION]` in `work/outstanding-bugs.md`, #171) | Make `Benzene.CloudService` load-bearing — it is deliberately sugar-only; no capability lives in it, manual composition of the same `Use*` calls remains the full-control path, and overrides are reported honestly (relocated paths flagged in the report, never refused) | The report's unsatisfied requirements are the exact to-do list; the probe audits from outside what the self-report claims |
| **Codegen & typed clients** | `Benzene.CodeGen.Core`/`.Client` — typed C# client-SDK generation from a service spec (schema closure, canonical JSON + contract hash); the `benzene` CLI (`Benzene.CodeGen.Cli` + `.Cli.Core`) with `spec`/`build`/`diff`/`healthcheck`/`cloud-service-profile`/`lambda-test-tool` commands over file/HTTP/Lambda/mesh spec sources — `diff`, `healthcheck` and `cloud-service-profile` are all genuine CI gates: each throws (→ non-zero exit) when its report trips the requested `--fail-on` threshold (`diff`'s default is `breaking`; `healthcheck`'s and `cloud-service-profile`'s default to failing on an unhealthy/not-satisfied target), never silently exiting 0 regardless of the target's real state; `build`'s generated C# always compiles even for a top-level polymorphic (`oneOf`) request/response schema (typed as the subtypes' shared base when discoverable, else `object`) and a schema property name that isn't already a valid C# identifier (e.g. `order-id` → `Orderid`) — and, for the same reason, a `$ref`'d schema id that isn't already a valid/Pascal-cased C# identifier (any bring-your-own-schema `SuppliedSchemaCatalog` id is caller-supplied, arbitrary text) generates a property/parameter *type* name formatted exactly the way its class declaration is, so the two can never diverge into an uncompilable mismatch (round 15, #240); an `enum`-shaped schema generates a real C# `enum` (a string enum gets `[JsonConverter(typeof(JsonStringEnumConverter))]` and its values as member names; an integer enum gets its values as explicit member values), not an empty class with no members (round 11, #166 — the prior empty class sent `"field":{}` on the wire and a real server rejected it with HTTP 400); topic-scoped client generation (`-topics ...`) narrows the generated DTOs (and the contract hash) to exactly what `SchemaClosure.Reachable` finds from the surviving topics, matching `AtomicClientSdkBuilder`'s per-topic narrowing, instead of emitting the whole service catalogue regardless of scope (round 11, #170); `diff`'s `SchemaCompatibilityComparer` recurses into `additionalProperties` (a `Dictionary<string, T>`-shaped schema's value type) the same way it already does for array `items`, so a breaking change entirely inside a map's value schema is caught rather than passing as "No changes" (round 11, #168); `Benzene.CodeGen.Markdown` renders a polymorphic property as its shared base type name or a `oneOf: {A|B}` union listing rather than leaving it blank; `Benzene.CodeGen.ApiGateway`'s `ApiGatewayBuilderV1` fails loudly (mirroring `ReflectionHttpEndpointFinder`'s duplicate-route check) when two topics collide on the same HTTP method+path, rather than emitting duplicate-key YAML and a corrupted CORS header; and `EventServiceDocumentDeserializer` builds a fresh schema catalogue per `Deserialize()` call, so reusing one instance across two documents that share a schema name (e.g. `diff`'s baseline vs. current) never lets the second document's schema silently resolve to the first's (round 11, #46); `spec`'s `OpenApiDocumentBuilder` fails loudly with a descriptive `InvalidOperationException` naming both the invalid HTTP method and the offending topic/path when a `[HttpEndpoint]`'s `Method` isn't one of OpenAPI's 8 supported operation verbs (`GET`/`PUT`/`POST`/`DELETE`/`OPTIONS`/`HEAD`/`PATCH`/`TRACE`, matched case-insensitively), rather than an opaque `KeyNotFoundException` naming neither (round 15, #241); `AddJsonEvent`'s example-driven schema inference tolerates an ordinary empty example array (`"tags": []`) with an untyped items placeholder, rather than crashing with `InvalidOperationException: Sequence contains no elements` (round 15, #242); `diff`'s `SchemaCompatibilityComparer`/`JsonSchemaComparer` correctly key a discriminator-mapped **inline** (non-`$ref`) `oneOf`/`anyOf` member by its actual mapping entry — pairing the *n*-th inline member with the *n*-th discriminator-mapping entry not already claimed by a `$ref` member — instead of a dead fallback comparison that always missed and silently degraded to pure array position, which reported a byte-identical reordering of inline discriminated-union variants as spurious breaking changes (round 15, #239); a baseline document missing `events`/`requests` entirely deserializes to empty arrays (matching the sibling `transports`/`tags` getters) instead of throwing `ArgumentNullException` (round 15, #243, minor); `Benzene.CodeGen.Build` — MSBuild targets compiling a client from a committed `{service}.spec.json` on every build; `Benzene.CodeGen.ApiGateway`/`.Markdown`/`.LambdaTestTool`; `Benzene.Azure.Function.SourceGenerators` — `AzureFunctionTriggerGenerator` emits the `[Function]`/`[…Trigger]` boilerplate class per transport from `[assembly: BenzeneXTrigger(...)]` declarations. Emission runs through one `RegisterSourceOutput` **per transport** (restored in WP-C, #38 — a prior round's merge into one shared output both re-emitted every trigger class on any single trigger edit *and* was the build-crash vector below), while the `BENZ0001` name-collision check stays a single **global** view combined into every transport's output, so a name shared *across* transports (e.g. a queue trigger and a Kafka trigger both named `"dup"`) is still caught, checked over the *full* declared set — including a trigger that carries its own diagnostic below (WP-C, #32: previously such a collision could report only that trigger's own diagnostic and silently ship the other side unchecked). Every trigger's `Location` is an *external* one (file path + span, no `SourceTree` — `AttributeReading.AttributeLocation`), not the tree-bound `SyntaxNode.GetLocation()` result — `TriggerInfo.Location` is deliberately excluded from the type's cache-driving equality, which without this let a stale, tree-bound `Location` survive into a later compilation that no longer contained its tree, crashing the whole incremental build with `ArgumentException` inside Roslyn's own suppression-checking on an ordinary, unrelated edit (WP-C, #38 — the reproduced build-crash finding; a Location with no tree reference at all has nothing left to go stale). Diagnostics, from one shared `DiagnosticDescriptors` table: `BENZ0001` (error, cross-transport collision, as above; the Function name is deliberately never auto-renamed the way the generated *class* name is, because it's externally meaningful — bindings, host.json, scale rules, the portal's identity for the function — so silently picking a different one would only move the failure from build time to deployment time); `BENZ0002` (error) when a `[assembly: BenzeneCosmosDbTrigger(...)]` omits the required `DocumentType`; `BENZ0003`–`BENZ0007` (error, WP-C #39) the same "required field missing" check extended to every other transport with one — ServiceBus (neither `QueueName` nor `TopicName` set), EventHub (`EventHubName`), Kafka (`Topic`), Queue Storage (`QueueName`), Blob Storage (`Path`); `BENZ0008` (error, WP-C #40) an explicitly-set empty/whitespace-only `Name`, across all 9 transports (distinct from the absent case, which correctly defaults); `BENZ0009` (warning, WP-C #42) a ServiceBus trigger setting *both* a queue and a topic — the queue still wins (unchanged precedence), this only surfaces what used to be a silent discard of the topic. All of `BENZ0002`–`BENZ0009` skip emitting the offending trigger except `BENZ0009`, which warns but still emits. The runtime client layer — `Benzene.Clients` (`IBenzeneMessageClient` + batch-send abstractions) with transport clients `Benzene.Clients.Http`, `.InProcess`, `.Aws.{Lambda,Sqs,Sns,EventBridge,StepFunctions}`, `.Azure.{ServiceBus,EventHub,EventGrid,QueueStorage}`, `.GoogleCloud.PubSub` | Ship `Benzene.CodeGen.Terraform` — deliberately excluded from the release (`IsPackable=false`) until its grow-or-split lifecycle call is made (it does escape `"`/`\` in every value it interpolates into a generated `.tf` string literal — `TerraformEventBridgeRuleBuilder`/`.LambdaBuilder`/`.LambdaEventBusPermissionsBuilder` all route through one `NameFormatter.EscapeHclString` helper, round 15, #244 — but remains otherwise unhardened, unpackaged experimental code). Auto-rename a colliding Function name to make the build pass — rejected: it's the user's name to fix, not the generator's to guess | Give the offending trigger a distinct name / its required field(s) / a non-empty `Name` per the diagnostic; the diagnostic's location points at the offending `[assembly: ...]` attribute's file/line/column |
| **AWS Lambda-clients** | `Benzene.Clients.Aws.Lambda` — two invocation shapes, both classifying the real outcome instead of assuming success: `AwsLambdaBenzeneMessageClient` (`IBenzeneMessageClient`) chooses `InvocationType` from `TResponse` (`Void` → `Event`/fire-and-forget, else → `RequestResponse`/awaited), and treats both a `RequestResponse` invoke's `InvokeResponse.FunctionError` (AWS returns HTTP 200 even when the target function threw) and an `Event` invoke's non-2xx `InvokeResponse.StatusCode` (thrown by the lower-level `AwsLambdaClient` as `AwsLambdaFunctionErrorException`/`AwsLambdaEventInvokeFailedException` respectively) as a caught failure (`ServiceUnavailable`), never a false `Accepted`; the lower-level `UseAwsLambda<T>()`/`LambdaContextConverter<T>` pipeline is fire-and-forget-only, sends `InvocationType.Event` (not the SDK's `RequestResponse` default), and maps a 2xx `InvokeResponse.StatusCode` to `Accepted`, anything else to a failure result. Plus `AwsLambdaHealthCheck` (reachability/active modes) — no internal timeout guard, relies purely on the processor's uniform timeout wrap, same shape as `SqsHealthCheck`/`StepFunctionsHealthCheck`. | A response payload through the `UseAwsLambda<T>()` pipeline shape — it is fire-and-forget by design; use `AwsLambdaBenzeneMessageClient` for a request/response call | See [AWS Lambda clients](clients.md#aws-lambda) for both shapes' exact semantics |
| **AWS Step Functions client** | `Benzene.Clients.Aws.StepFunctions` — `StepFunctionsClient.StartExecutionAsync(message, executionName?)` makes a Step Functions start genuinely idempotent: on `ExecutionAlreadyExistsException` (an execution-name collision), it calls `DescribeExecution` and compares the existing execution's `Input` to this call's serialized input as an exact string — a match is a true idempotent duplicate (`Accepted`); a mismatch is a `Conflict` failure result (the caller's payload was **not** started, left untouched, and the caller is told so rather than getting a false-positive `Accepted`). `SanitizeExecutionName` keeps the mapping from idempotency token to execution name collision-resistant: whenever character replacement or the 80-character cap would otherwise make the sanitized name diverge from the original, the result is the sanitized name truncated to 71 characters plus an 8-hex-character `SHA-256(original name)` suffix, so distinct originals that happen to sanitize or truncate alike still land on distinct execution names — required for the `DescribeExecution` compare above to mean anything. | Reconstruct or resume the workflow itself — this client only *starts* an execution; see the Sagas / workflows row | Callers relying on the idempotency name must serialize input deterministically (same logical input → the same serialized string); requires `states:DescribeExecution` alongside `states:StartExecution` — see [AWS IAM permissions](aws-iam-permissions.md) |
| **Caching** | `Benzene.Cache.Core` — provider-agnostic cache-aside/write-through abstractions + base classes your handler code calls directly, plus a cache health check; `Benzene.Cache.Redis` — the Redis provider. Every member of `ICacheService`/`ICacheEntry<T>`/`ICacheWriteActions<T>`/`ICacheInvalidateActions` takes a `CancellationToken` (optional, default), threaded into the Redis calls via `Task.WaitAsync` (StackExchange.Redis has no native per-call cancellation) and into `RedisCacheService`'s own connect, which previously had no deadline of any kind; `LazyLoadAsync`/`WriteThroughAsync` also take an optional per-call `expireIn`, flowed through to the underlying `SetValueAsync`. Negative caching is supported: an explicit `SetValueAsync(default)` is a real, repeatable cache hit on the next `LazyLoadAsync` (presence, not value-nullness, decides a hit) rather than a permanent miss that re-runs the database read every time. A write-through cache-sync failure (exception, or a provider honestly returning `false`) *after* the database write already committed is logged and swallowed, never surfacing as that operation's own failure — the database write is the source of truth. `RedisCacheService` accepts a constructor-injected `ISerializer` (falling back to a shared default, not a fresh instance per cache entry) and guards a `RedisSetup`/connect call arriving after `DisposeAsync` with `ObjectDisposedException` rather than silently leaking a new connection. `RedisMultiKeyActions` issues its per-key writes concurrently (every key is always attempted, independent of another key's failure) and its multi-key invalidate as a single atomic `DEL` | Cache middleware or HTTP response caching — what and when to cache is a handler-level decision; no pipeline knob decides it for you. Automatic negative caching of a database miss (`LazyLoadAsync` still only writes a *successful* read's payload back; a caller decides for itself whether a `null`/not-found result is worth caching and calls `SetValueAsync(default, ...)` itself). Cancel the caller-supplied `modifyDatabaseFunc`/`databaseReadFunc` delegates passed to `WriteThroughAsync`/`WriteThroughInvalidateAsync`/`LazyLoadAsync` — the `CancellationToken` parameter bounds only the cache's own read/write/invalidate I/O, never these zero-arg delegates (a deliberate scope boundary, not an oversight: retrofitting a `Func<CancellationToken, Task<TResult>>` shape onto them is a second, independently-justified breaking change); a caller needing its own database call cancelled observes the ambient token itself, same as calling that database directly outside the cache | Extend the `Benzene.Cache.Core` base classes for another provider |
| **Validation** | Three adapters sharing one short-circuit failure contract: `Benzene.FluentValidation` (runs the app's own DI-registered `IValidator<T>` before the handler), `Benzene.DataAnnotations` (BCL attributes, deliberately minimal), `Benzene.JsonSchema` (raw body vs. a JSON Schema generated from the handler's request type, or supplied, resolved against the version the request actually declares on a multi-version topic — see `GetVersionedTopic` in [Message Handlers](message-handlers.md)); seams in `Benzene.Abstractions.Validation`. **All three now honour the same failure-status override:** each resolves an optional `IValidationStatusMapper` and, when one is registered (only `Benzene.FluentValidation` ships an implementation, `DefaultValidationStatusMapper`) and the resolved handler carries `[ValidationStatus]`, uses its status instead of the default `ValidationError` — register the mapper once (or your own `IValidationStatusMapper`) and every adapter honours it identically; absent a registered mapper, each keeps its pre-existing default unchanged | Define a validation DSL of its own — you write ordinary FluentValidation/attribute/schema rules; Benzene only wires them into the pipeline and maps failures to a validation-status result. **The three adapters share the short-circuit *contract*, not the *coverage*:** `DefaultJsonSchemaProvider`'s generated schema is type-shape-only — it does not understand `System.ComponentModel.DataAnnotations` attributes (`[Required]`/`[Range]`/`[MinLength]`, ...), so a DTO validated meaningfully under `Benzene.DataAnnotations`/`Benzene.FluentValidation` can pass `Benzene.JsonSchema`'s default provider with no warning; see the package's README for the gap and how to close it (a supplied schema, or `Json.Schema.Generation`'s own attributes). | Any other library via a small middleware honouring the same short-circuit contract |
| **Versioning** | Both spec mechanisms. Handler-version dispatch: `(topic, version)` handler registration selected by `IVersionSelector` — the default `VersionSelector` (`Benzene.Core.MessageHandlers`) uses the requested version on an exact match, else the highest available by ordinal comparison — with the version read via the transport-neutral header-fallback `IMessageVersionGetter` and joined into the topic once, inside `IMessageGetter<TContext>.GetTopic` itself (cached per message) — every reader of the topic (the router, `UseMeshTrace`, health checks, `Benzene.CloudService` dispatch, ...) sees the same version-resolved `ITopic`, not only the router. Two concrete implementations carry this join, not one: the open-generic `MessageGetter<TContext>` facade (`Benzene.Core.MessageHandlers`) and `BenzeneMessageGetter` (`Benzene.Core.MessageHandlers.BenzeneMessage`) — the closed-type registration that wins DI resolution for the `BenzeneMessage` transport nearly every test in this repo uses, and so needed the identical fix independently. Payload casting: `Benzene.Core.Versioning` — up- *and* downcasting with auto-composed multi-step chains (a registered shortcut caster is preferred), serializer-agnostic (JSON/XML/MessagePack/Avro); `Benzene.Grpc.Versioning` adds the request side over gRPC | Response-side downcasting over gRPC (see the gRPC row) | Swap in your own `IVersionSelector` for a different selection policy (e.g. strict exact-only) |
| **Serialization beyond JSON** | `Benzene.Xml`, `Benzene.MessagePack`, `Benzene.Avro` — negotiable `IMediaFormat`s alongside JSON via request/response content negotiation; `Benzene.NewtonsoftJson` — a drop-in Json.NET `ISerializer` for apps needing Json.NET semantics. Untrusted-input hardening: `Benzene.Avro` bounds both allocation (`BoundedBinaryDecoder` rejects a hostile length prefix before allocating) and recursion depth (`AvroOptions.MaxDepth`, default 500, on both serialize and deserialize — a self-referencing/deeply-nested schema or object graph throws a catchable `AvroPayloadTooDeepException` instead of an uncatchable CLR stack overflow); `Benzene.Xml`'s reader prohibits DTDs (entity-expansion DoS) and accepts a UTF-8-BOM-prefixed body like every other transport does. The default `System.Text.Json` serializer and `Benzene.NewtonsoftJson` now agree on the wire form for `NaN`/`Infinity`/`-Infinity` doubles (both emit the quoted strings `"NaN"` etc.; STJ used to throw) | Carry raw binary bodies — every Benzene transport body is a string today, so MessagePack/Avro Base64-armor their binary wire form (a stated boundary of the current wire contract, not an accident). **`Benzene.Avro` has no schema-evolution support** — the writer and reader use the same resolved schema (no schema exchanged on the wire), so reader and writer must share the exact same field shape; a field added/removed/reordered between producer and consumer versions is not a supported change (a same-type field-count/order mismatch is now detected and reported as `AvroSchemaMismatchException` rather than silently misread or an opaque exception, but this is detection, not resolution) | Implement `ISerializer`/`IMediaFormat` for any other format |
| **Event sourcing** | `Benzene.EventSourcing` — a deliberately tiny `IEventStore` seam: `AppendAsync(streamId, expectedVersion, events)` (optimistic concurrency, throws `EventStoreConcurrencyException` — now with an optional inner-exception constructor preserving the underlying store failure — if the stream moved; a negative `expectedVersion` is rejected with `ArgumentOutOfRangeException` before either store touches storage) and `ReadAsync(streamId, fromVersion)` (ordered events), serialization-agnostic (`EventEnvelope`/`StoredEvent` carry an opaque `Payload` + `EventType` discriminator, no JSON opinion baked in); `InMemoryEventStore` for a single process/tests — appends are atomic across a batch (a mid-batch failure leaves the stream and its dictionary entry untouched, never a partial write or a leaked empty-stream registration for a rejected unknown stream id), observe `CancellationToken`, and enforce the same 100-event-per-append ceiling as the DynamoDB store; `Benzene.EventSourcing.DynamoDb` — the distributed store for a fleet, its constructor fail-fast validating the table/key configuration (non-empty table and key attribute names, distinct partition/sort keys, neither colliding with the reserved `eventType`/`payload`/`timestamp` attributes): one item per event keyed `(streamId, version)`, `AppendAsync` is a single `TransactWriteItems` — carrying a deterministic `ClientRequestToken` derived from the append's own content, so a retried request lands as the same idempotent attempt rather than a fresh conflicting one — with each `Put` conditional on that slot not existing (`attribute_not_exists`), plus (when `expectedVersion > 0`) a `ConditionCheck` that the stream is genuinely *at* `expectedVersion` (`attribute_exists`), not merely that the slots above it are free — so racing writers on the same expected version can't both win, and an `expectedVersion` ahead of the real head can no longer silently gap the stream; a `TransactionCanceledException` is inspected item-by-item and translated to `EventStoreConcurrencyException` only for a genuine `ConditionalCheckFailed`/`TransactionConflict` reason, with the post-conflict "actual version" diagnostic read run under its own `CancellationToken.None` and degrading to `ActualVersion = -1` on its own failure rather than ever replacing the real conflict; an empty append still runs the same concurrency check as a non-empty one (via a direct consistent read, matching `InMemoryEventStore`); a read event with a missing or non-string `eventType`/`payload` attribute throws rather than silently defaulting to an empty string | An aggregate framework, rehydration, snapshots, replay, or projections — the store is only the append-only log; folding events into state is a pure app-level function by design, kept minimal on purpose. Provision or create the DynamoDB table — the consumer registers `IAmazonDynamoDB` and provisions the composite-key table itself | Rehydrate by folding `ReadAsync` in your command handler, then `AppendAsync` at the version you read; build CQRS projections by consuming the DynamoDB table's own stream via `Benzene.Aws.Lambda.DynamoDb` (`[Message("table:INSERT")]`); evolve event shapes with `AddPayloadVersioning` (upcast on read) — events are immutable, never rewritten |
| **Map-reduce (scatter-gather)** | `Benzene.MapReduce` — `IBenzeneMessageSender.ScatterGatherAsync<TShard,TPartial,TAccum>(topic, shards, seed, reduce, options?)`: the map is one `SendAsync` per shard run concurrently through `BoundedFanOut.WhenAllAsync` (bounded by `MaxDegreeOfParallelism`, results folded in source order; each shard resolves through the routing table, so on AWS it's a Lambda-to-Lambda invoke), the reduce is an app-owned fold over the successful partial responses; `PartialFailureMode.ThrowOnAnyFailure` (default — throws `ScatterGatherPartialFailureException` so an incomplete total is never mistaken for complete) or `.BestEffort` (reduces over successes, reports `FailedShards`/`IsComplete`). Each failed shard carries its own failure reason — `FailedShards` is `IReadOnlyList<FailedShard<TShard>>` (shard + the `Exception?` its worker call threw, `null` for an unsuccessful-result failure), and a thrown `ScatterGatherPartialFailureException` aggregates every reason onto `InnerException` (an `AggregateException`) plus a per-shard `Failures` list, so which shard failed and why is diagnosable from the exception alone | Ship a dedicated scatter-gather engine or framework — this is the thin, supported composition of parts Benzene already has (`SendAsync` + `BoundedFanOut`), kept to one method, options, and a result on purpose. Built-in hierarchical fan-out for very large shard counts | For very large fan-outs, shard hierarchically yourself — a coordinator scatters to partition workers that each scatter and reduce locally, the same `ScatterGatherAsync` call at each level |
| **Rate limiting** | `Benzene.RateLimiting` — `RateLimitingMiddleware<TContext>` built directly on `System.Threading.RateLimiting`'s abstract `RateLimiter` (BYO fixed/sliding window, token bucket, concurrency, or custom limiter); never queues (`QueueLimit = 0` on every built-in) so a rejection is immediate, not memory pressure; convenience entry points `UseFixedWindowRateLimiting`, `UseTokenBucketRateLimiting`, `UsePayloadSizeRateLimiting` (a bytes-per-second token bucket costing each message its UTF-8 body size, with a cheap `Content-Length`-based pre-check — a rate/CPU saving, **not** a memory bound: see [Rate Limiting](rate-limiting.md) for why); **per-caller partitioning** via `PartitionedRateLimitingMiddleware<TContext>`/`UsePartitionedRateLimiting` over a BYO `PartitionedRateLimiter<TContext>`, so one abusive caller can't exhaust the shared budget for everyone else (the key is caller-supplied and therefore spoofable, but strictly better than the single shared limiter every other entry point still uses by default); a rejected message short-circuits with `TooManyRequests` (→ HTTP 429), carrying the limiter's retry-after metadata in both the error message and a `Retry-After` response header when the limiter supplies it, and logging a warning when an `ILogger` is registered; the three convenience limiters' disposal is owned by the DI container (a caller-supplied/BYO limiter's disposal is always the caller's) | Authoritative, fleet-wide rate limiting — every limiter instance is **per service instance**: a fleet of N instances admits up to N× the configured rate, and serverless scale-out multiplies it further. This package protects one instance from being a free denial-of-service/cost-amplification vector; it is not traffic shaping. `UsePayloadSizeRateLimiting` is a rate bound on oversized payloads, not a hard memory cap — ASP.NET Core hosts buffer the request body before this middleware ever runs | Put authoritative rate limiting at the gateway (API Gateway, APIM, ingress) in front of every instance; put a host-level body-size cap (Kestrel's `MaxRequestBodySize`, a gateway limit) in front of an endpoint where payload size is a genuine memory-exhaustion concern; see [Rate Limiting](rate-limiting.md) |
| **Conformance** | `test/Benzene.Conformance.Test` runs the vendored language-neutral fixtures in `test/conformance-fixtures/` (a `SPEC_VERSION`-stamped snapshot of the main repo's `docs/specification/conformance/`): envelope, HTTP + gRPC status mappings, status vocabulary, problem details, contract document/hash, and the mesh descriptor/trace/issue/collector/version-order shapes (`transport-metadata-cases.json` runs in `test/Benzene.Core.Test/Conformance/`); the `conformance-drift-check.yml` workflow re-verifies the vendored snapshot against the canonical repo | Edit a fixture to make this port pass — the fixture is the neutral truth | A mismatch is a bug here or a deliberate spec change there, never a fixture patch |

## Why "we don't do that" is a feature

Every deliberate "no" above buys you something: you keep the full power of the tool you chose,
you're never blocked by a leaky abstraction, and the surface you *do* depend on stays small and
stable. When you need something Benzene doesn't ship, the extension model (custom middleware,
custom pipes, the getter/setter mapper pattern) is a supported, documented path — see
[Middleware](middleware.md) and [Common Middleware](common-middleware.md).

If you find yourself wishing Benzene abstracted a transport or a database away, that's usually the
signal to reach for the SDK directly inside your handler — which is exactly where Benzene's design
wants that code to live.
