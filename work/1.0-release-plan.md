# Benzene 1.0 Release Plan — Master Source of Truth

**Created:** 2026-07-18
**Owner:** Maintainer (with AI agent execution)
**Status:** ACTIVE — this is the driver for the 1.0 release. We iterate through it.

> ## Why this document exists
> A five-lens release-readiness assessment (AWS, Azure, ASP.NET/self-hosted/infra, core
> capabilities, OSS-release) was run 2026-07-18, each lens verifying claims **against actual
> `src/` code** rather than trusting the older `work/` roadmaps. That assessment found the older
> readiness docs (`1.0-readiness-checklist.md`, `1.0.0-release-checklist.md`, `api-surface-review.md`,
> the per-area `*-roadmap-1.0.md` files) are **systematically stale** — a mix of resolved, stale,
> and aspirational claims with no reliable marker, citing phantom types and already-fixed bugs.
> **They must not drive the release decision.** This document supersedes them as the release
> driver. The per-area roadmaps remain useful as history, not as truth.
>
> **The release sign-off is code-driven, not doc-driven.** Every checkbox below is closed by
> reading/running code, not by a doc saying it's done.

---

## 1. The release decision (settled)

**Big-bang release, not a narrow-core release.** The core packages ship 1.0.0 **together with**
the AWS and Azure packages, because nobody adopts the core without a cloud host — the core alone
has no audience. This raises the bar: it means the AWS and Azure surfaces (ingress **and** egress)
and the capability packages must all be either genuinely 1.0-ready or **honestly scoped**, because
none of them get to hide behind a `-preview` tag.

Consequence: the correct 1.0 is **not** "ship less." It is "ship the whole primary surface, with
documentation that tells the exact truth about what each package does and does not do, and with
the deliberate non-goals stated as non-goals rather than discovered as gaps."

Scope of this release: **AWS, Azure, ASP.NET Core, self-hosted.** Google Cloud and Cloudflare are
explicitly **out of scope** for 1.0 — they are lesser offerings today and should be marked
experimental/community and held out of the 1.0 cut (or clearly labelled), not blocking it.

---

## 2. Design philosophy — the decision framework

Every "should Benzene do X?" question below is answered by these principles. They are the reason
some findings are *build-it* and others are *document-it-as-a-non-goal*.

1. **Abstract at the business-logic boundary, never at the transport.** Benzene's abstraction
   lives in the message handlers — write your handler once, host it anywhere. It deliberately does
   **not** abstract the transport itself. This is the opposite of Dapr: Dapr hides the third party
   behind a generic interface (a "queue", a "state store"), and in doing so hides the third party's
   *core capabilities*. If you abstract SQS behind a generic queue, you lose the SQS-specific
   features that were the reason to choose SQS. Benzene never does this — you keep full access to
   the underlying SDK and all its cloud-native capabilities. We abstract *where the business logic
   is*, so the handler is portable, while the transport stays fully itself.

2. **Bring your own code for anything that isn't a transport.** Benzene does **not** get involved
   in database access. A database is not a transport. This is why Cosmos DB has a change-feed
   *ingress* (the feed is an event stream) but **no egress** (writing to Cosmos is database access —
   your handler uses `CosmosClient` directly). Same for Blob storage as a store, EF Core, etc.
   Benzene provides the pipe in; persistence is yours.

3. **If we don't ship a cloud offering out of the box, rolling your own is easy and first-class.**
   The extension model (custom middleware, custom pipes into Benzene, the getter/setter mapper
   pattern) is a supported public path, not an escape hatch. "Benzene doesn't have an adapter for
   X" should always be answered by "…and here's the 20-line middleware that adds it," documented.

4. **Let people use the libraries they already know.** Where a best-in-class library exists (Polly
   for resilience, FluentValidation for validation, the cloud SDKs), Benzene's job is to give it a
   clean place to plug into the pipeline — **not** to wrap it in a Benzene abstraction that hides
   its capabilities (see principle 1). A "Benzene.X integration" package exposes X, it doesn't
   replace X.

5. **Honesty over false capability.** For a hexagonal framework, the package documentation *is*
   the API contract — it is the first thing an evaluator reads. Docs that claim capabilities the
   code doesn't have are the single biggest launch risk. Where Benzene deliberately doesn't solve
   something (cross-process idempotency, database access, transport abstraction), we say so plainly
   **and** show how to solve it outside Benzene. A stated non-goal builds trust; a discovered gap
   destroys it.

6. **Some problems cannot be solved at runtime inside Benzene, and that's fine.** Benzene instances
   are independent processes (e.g. separate Lambda invocations) that don't know about each other.
   Problems requiring cross-instance coordination (distributed idempotency, exactly-once) can't be
   solved by Benzene alone; bolting on shared state can just move the problem to a race condition.
   The honest answer is an external store with atomic semantics plus naturally-idempotent handler
   design — documented, not pretended away.

---

## 3. The assessment in brief (the six convergent themes)

All five lenses independently converged on these. Full evidence is in §7 (Appendix: findings by
lens).

- **T1 — The docs oversell the code.** Verified false claims across many `CLAUDE.md`/package docs:
  `Benzene.Resilience` advertises "Polly, retry, circuit breaker, timeout, bulkhead" but ships
  retry-only with no Polly; `Benzene.Aws.Sqs` claims FIFO/dedup/batch that don't exist; ApiGateway
  docs claim HTTP API v2 and reference a deleted `Cors` type; `Client.Http`/`Cache.Redis`/`Results`
  docs name capabilities/types not in code. **The #1 launch risk. Highest leverage, lowest effort.**
- **T2 — The release machinery can't cut a 1.0.** License metadata says Apache-2.0 while the repo
  is MIT (verified: `Directory.Build.props:8` vs `LICENSE`/README badges); committed repo-root
  junk leaking an internal name (`Output/…PlatformPedalCore…`, `86 lines changed`, neither
  gitignored); the publish pipeline hardcodes `-alpha` and a four-segment version so it can never
  emit `1.0.0` (`deploy-benzene.yml:45`); examples aren't compile-gated except Azure; GitHub-broken
  extensionless doc links; missing SECURITY/COC/templates; thin NuGet metadata, no SourceLink.
- **T3 — API-freeze warts become permanent.** Oversized public surface in `*.MessageHandlers`
  (impl types public — can't be made `internal` post-1.0 without a major bump); naming warts
  (`MessageMessageHandlerResultSetter` ×20, `IRequestMapperThunk`); `IDependencyWrapper<T>` already
  `[Obsolete]` in the root package. Big-bang makes these **release-blocking**, since everything
  freezes at once.
- **T4 — Capability packages are abstraction+in-memory-only.** Idempotency (in-memory single-
  process only), Saga (no durable resume), SchemaRegistry (compat checker is a textual no-op),
  Resilience (retry-only). Under big-bang these can't be `-preview` — they must be **honestly
  scoped** per principles 5 & 6.
- **T5 — Egress is the real functional gap.** Azure has **no outbound story at all**; AWS clients
  exist but are lumped in one package (pulling in SQS to use SNS) and Step Functions is a stub.
  This is genuine missing capability → **build it** (§5).
- **T6 — Async-transport observability is weakest where Benzene differentiates.** Distributed
  tracing is HTTP-only; SQS/SNS/Kafka/Event Hub inbound trace extraction isn't implemented;
  `invocationId` dropped in per-message sub-pipelines. The queue transports that *are* the pitch
  have the poorest correlation.

**Genuinely strong (showcase, don't re-litigate):** core middleware pipeline; `Benzene.Core.Versioning`;
results/media-negotiation; `Benzene.Auth.OAuth2` authN (RFC 8725 algorithm-confusion protection,
no token oracle); SQS partial-batch-failure; DynamoDB/Kinesis streaming semantics; the Azure
trigger matrix + Event Grid CloudEvents + Service Bus explicit-ack + self-hosted workers + managed
identity; k8s health probes; the self-hosted worker concurrency/lifecycle rewrite; the DI-scope
leak fix; System.Text.Json default.

---

## 4. Tiered worklist (we iterate through this)

Each item is a unit of work with a done-when. Tier order is execution order.

### Tier 0 — Release machinery (blocks calling it "1.0" at all; mostly mechanical, ~days) — ✅ DONE 2026-07-18

> **Release dry-run — verified 2026-07-19.** Replicated the `deploy-benzene.yml` pack step end-to-end
> (`dotnet pack Benzene.sln -c Release -p:PackageVersion=1.0.0`, the exact command a `stable`-channel
> cut runs once `version.txt` is bumped). Result: **135 packages + 134 symbol packages produced, zero
> `NU5xxx` packaging warnings/errors.** Verified across **all 135** `.nuspec`s: `version=1.0.0` and
> `license` = MIT expression (0.1); each carries the description, tags, `projectUrl`, README (packed
> in — no NU5039), and a commit-pinned `<repository>` for SourceLink, with a `.snupkg` (0.7 — the lone
> exception is `Benzene.CodeGen.SourceGenerators`, a Roslyn analyzer that correctly ships without
> symbols). No `test/` project leaked into the package set. So the pipeline **can** emit a clean
> `1.0.0` (0.3). **Two pre-tag notes for the maintainer:** (a) `version.txt` is still pre-1.0 (`0.0.3`
> as of 2026-08-20) — bump it to `1.0.0` when cutting the tag (the `stable` channel packs exactly
> `version.txt`); (b) no
> `PackageIcon` is set (deliberate — no PNG logo asset in the repo yet; add one + set `PackageIcon`
> centrally for nuget.org polish — non-blocking).

- [x] **0.1 License** — set `Directory.Build.props` `PackageLicenseExpression` to `MIT` (match
  `LICENSE` + README). Verify no other `.props`/`.csproj` overrides it. *Done when: a packed
  `.nupkg`'s license metadata reads MIT.*
- [x] **0.2 Repo-root junk** — delete the tracked `86 lines changed` file and the `Output/` folder
  (leaks `PlatformPedalCore`); add `Output/` (and the stray pattern) to `.gitignore`. *Done when:
  `git ls-files` shows neither and the internal name appears nowhere in the tree.*
- [x] **0.3 Version pipeline** — rework `deploy-benzene.yml` to emit real SemVer (`1.0.0` stable,
  `1.0.0-rc.N`/`-preview.N` for prerelease — three-part, not `x.y.z.N-alpha`); reconcile with
  `VERSIONING.md`; set `version.txt` when we tag. *Done when: the pipeline can produce `1.0.0` and
  matches its own policy doc.*
- [x] **0.4 Examples compile gate** — add a `dotnet build Benzene.Examples.sln` job to
  `build-benzene.yml` (mirror the existing `azure-example-build` job) so AWS/Asp examples can't
  ship broken. *Done when: CI builds the examples solution on every push/PR.*
- [x] **0.5 GitHub-correct doc links** — convert extensionless internal links (`](getting-started-aws)`)
  to `.md`-suffixed relative links so they resolve on GitHub; adjust the website generator to strip
  extensions rather than requiring their absence. *Done when: internal doc links work when browsing
  the repo on GitHub.*
- [x] **0.6 Project-health files** — add `SECURITY.md` (disclosure contact), `CODE_OF_CONDUCT.md`,
  `.github/ISSUE_TEMPLATE/`, `PULL_REQUEST_TEMPLATE.md`. *Done when: GitHub Community Standards
  checklist is green.*
- [x] **0.7 NuGet metadata + SourceLink** — centrally in `src/Directory.Build.props`: SourceLink,
  `PackageReadmeFile`, `PackageIcon`, `PackageProjectUrl`, `RepositoryUrl`, symbol packages
  (`snupkg`); require a `<Description>` and `<PackageTags>` per public package. *Done when: a packed
  package shows description/icon/tags/repo link and supports source-stepping.*

### Tier 1 — Truth & freeze (highest leverage)

- [ ] **1.0-SPEC — Specification review outcomes (ADDED 2026-07-25, CRITICAL TO GO-LIVE).**
  The maintainer's pass over `docs/specification/` produced a backlog
  (`work/spec-review-2026-07-25.md`) that is almost entirely **wire contracts, header names, topic
  ids and the public result model** — precisely what cannot change after the tag without a major
  version. **These block the tag**, not because the spec is wrong today, but because getting them
  wrong is permanent. Sub-items and their state:
  - [x] **Naming principle** — RULED and accepted (`work/benzene-naming-principle.md`): mark
    Benzene's names where the namespace is shared, don't where it's already ours, never rename
    what we borrow.
  - [x] **Error payload + result model** — **DONE (both halves shipped; verified against source
    2026-08-20)**. Model half: `BenzeneError` ships in `src/Benzene.Abstractions/Results/BenzeneError.cs`
    and `IBenzeneResult.Errors` is `IReadOnlyList<BenzeneError>`. Wire half: `src/Benzene.Results/
    ProblemDetails.cs` is a real RFC 9457 document with `ProblemTypes.cs`, rendered as
    `application/problem+json` by `SerializerResponseRenderer` and as gRPC rich errors by
    `GrpcMethodHandler`; `test/Benzene.Conformance.Test/ProblemDetailsConformanceTest.cs` is green.
    The historical statement of the two halves follows. The model half was ruled and approved
    (`work/benzene-result-errors-ruling.md`: `IBenzeneResult.Errors` →
    `IReadOnlyList<BenzeneError>`, `BenzeneError` in `Benzene.Abstractions`, string form to an
    `ErrorMessages()` extension; measured blast radius 8 `src` reads / 23 test assertions / 0 live
    example hits / 2 private nested implementers). The wire half is proposed and unblocked but
    **not yet ruled** (`work/error-payload-proposal.md`: withdraw the false RFC 7807 claim, drop
    always-null `type`/`title`/`instance`, fix the unimplementable "recover errors from detail"
    rule, add the optional structured `errors` array). **This is the last change to the result
    model before the tag** — deferring it doesn't defer the decision, it makes it, since a member's
    type cannot change in a minor.
  - [x] **Reserved-topic `benzene:` prefix** — **DONE 2026-07-25**, recorded in
    `work/topic-prefix-migration.md` (itself marked DONE). Applied the accepted principle across the
    widest blast radius on this list (constants, mesh wire topics, conformance fixtures, spec, mesh UI
    filter, examples, deployed contracts).
  - [ ] **Header tiering** (core / profile / add-on / binding) — mostly doc restructuring, plus the
    free-now-never-again `benzene-version` precision rename while it is still unimplemented.
  - [ ] **`_benzeneHeaders` → `benzene-headers`** — a wire contract, so critical, but **deferred by
    agreement to after the repo split** (`work/repo-split-plan.md`) to avoid colliding with the
    in-flight .NET file moves. Plan ready to execute: `work/benzene-headers-plan.md` Phase A.
  - [x] *(Not go-live critical)* `CancellationToken` design — **DONE**: all five phases of
    `work/cancellation-design.md` shipped (core plumbing, SQS/GCP/Azure-Functions wiring,
    `Benzene.Resilience`'s `UseTimeout`, and docs/example adoption in `docs/message-handlers.md`
    §Cancellation, `docs/common-middleware.md` §UseTimeout, `docs/middleware.md` §Middleware and
    cancellation, and `examples/Asp/Benzene.Example.Asp`'s `/slow` route).
  - [ ] *(Not go-live critical)* The one-switch correlation add-on, and the packed-headers
    generalisation (`benzene-headers-plan.md` Phase B) — still pending; additive, can follow the tag.

  Also surfaced by this work: `docs/migration-alpha-to-1.0.md` — **written since; it exists**
  (its own header states the version it migrates to, which must be kept in step with `version.txt`).

- [x] **1.1 Doc-truth sweep (THE big one).** Reconcile every `src/**/CLAUDE.md` and package doc
  with actual code. Delete/correct every false capability claim and dead type reference. Known
  offenders to fix first: `Benzene.Resilience` (retry-only, no Polly), `Benzene.Aws.Sqs`
  (no FIFO/dedup/batch), `Benzene.Aws.Lambda.ApiGateway` (deleted `Cors` type; HTTP API v2 claim —
  see 2.x), `Benzene.Clients.Http`, `Benzene.Cache.Redis`, `Benzene.Results` (wrong type names),
  the four already-fixed legacy Azure ones are done. Also correct wrong type names throughout AWS
  docs (`SqsContext`→`SqsMessageContext`, etc.). *Done when: a spot-check of any package doc against
  its code finds no false claim.* **This is the release's highest-ROI work.**
  **DONE 2026-07-19:** all **111** `src/**/CLAUDE.md` package docs audited against their code in six
  clusters (core/abstractions, AWS ingress, clients/egress, Azure+Google ingress, capability/infra,
  hosting/health/mesh), each type/interface/method/extension/enum/version-pin cross-checked. The
  named offenders were already corrected by earlier Tier-1/3 work and verified accurate this pass:
  `Benzene.Resilience` correctly says retry-only/no-Polly (and now points at the real sibling
  `Benzene.Resilience.Polly` package); `Benzene.Aws.Sqs` documents FIFO/dedup/batch as **not**
  supported; `Benzene.Aws.Lambda.ApiGateway` states CORS lives in `Benzene.Http` and that
  `APIGatewayHttpApiV2ProxyRequest` is not referenced; `Benzene.Results`/`Benzene.Clients.Http`/
  `Benzene.Cache.Redis` type names all match; AWS docs already use `SqsMessageContext` (not
  `SqsContext`). Only **2** live findings surfaced, both the same stale rename — the code type
  `IRequestMapperThunk`/`RequestMapperThunk<TContext>` → `IDeferredRequestMapper`/
  `DeferredRequestMapper<TContext>` (the "Thunk" jargon dropped in code per the api-freeze proposal,
  not propagated to docs). Fixed in 4 files: `Benzene.Abstractions.MessageHandlers/CLAUDE.md`,
  `Benzene.Core.MessageHandlers/CLAUDE.md`, `docs/specification/versioning.md`,
  `docs/message-handlers.md` (also corrected the interface arity — `IDeferredRequestMapper` is
  non-generic, the doc had `<TContext>`). Remaining `Thunk` mentions live only in `work/*` and
  `docs/plans/*` (historical planning/design records — the api-freeze proposal deliberately documents
  the old→new rename), left as-is. A spot-check of any package doc against its code now finds no false
  claim.
- [x] **1.2 API-freeze decision & surface reduction.** Make the calls that become permanent at 1.0:
  which `*.MessageHandlers` implementation types should be `internal` (they can't be hidden later);
  whether to rename the naming warts now (`MessageMessageHandlerResultSetter` ×20 → single-`Message`;
  `IRequestMapperThunk`) or accept them forever. *Done when: the public surface of the core packages
  is one we can commit to for the whole 1.x line, reviewed type-by-type.*
  **Note (2026-07-19):** the `IDependencyWrapper<T>` removal this item used to list is **already done**
  — it was deleted 2026-07-17 with the rest of the alpha-era outbound-client decorator mechanism (see
  `CHANGELOG.md` `### Removed`, and `work/1.0-api-freeze-proposal.md` Decision D "ALREADY DONE"); a
  repo-wide grep finds no `IDependencyWrapper` in code. Its role is covered by outbound middleware
  (`.UseRetry`/`.UseResiliencePipeline`/`.UseCorrelationId`/`.UseW3CTraceContext` on `OutboundContext`).
  **Maintainer decision (2026-07-19): leave it removed** — do not restore it.
  **Naming warts (2026-07-19): already resolved in code — verified, nothing to rename.** Both named
  warts are already fixed: (B) the double-`Message` `MessageMessageHandlerResultSetter` family is now
  the single-`Message` form across every transport (`SqsMessageHandlerResultSetter`,
  `KafkaMessageHandlerResultSetter`, … + bases `MessageHandlerResultSetterBase`/
  `ResponseMessageHandlerResultSetterBase`/`DefaultMessageHandlerResultSetterBase`, interface
  `IMessageHandlerResultSetter`) — a repo-wide grep finds **zero** `MessageMessageHandlerResultSetter`
  in `src`/`test`; and (C) the non-generic dispatch interface is now
  `IMessageHandler.HandleAsync(IDeferredRequestMapper)` — the `HandlerAsync` typo and the `Thunk`
  jargon are both gone (the only remaining `HandlerAsync` in the tree is the deliberate AWS Lambda
  `FunctionHandlerAsync` entry point, kept per the proposal). Matches `work/1.0-api-freeze-proposal.md`
  Decision B "DONE (rename)" (its stale `[ ]` checkboxes for B/C reconciled). Remaining `…Message…`
  mentions are only historical `work/*`/`CHANGELOG.md` records.
  **Internal-visibility freeze (Decision A) — DONE 2026-07-19, reviewed type-by-type.** Worked the
  proposal's internal-candidate list against actual cross-assembly usage (maintainer said "change to
  internal"). Outcome: **8 candidates were already `internal`** (`Cache`/`Composite`/`Dependency`
  `MessageHandlersFinder`, `MessageHandlerNoResultWrapper`, `PipelineMessageHandlerWrapper`,
  `BenzeneMessageResponseAdapter`, `DefaultResponseStatusHandler`, `DeferredRequestMapper<TContext>`);
  **2 classes newly internalized** — `MessageHandlerFactory` and `MessageHandler<TRequest,TResponse>`
  (only `Benzene.Core.MessageHandlers` + the IVT'd `Benzene.Test` reference them; the source generator
  emits neither — it only detects the public generic `IMessageHandler<,>`). **5 must stay public for a
  real reason, not an oversight** — documented so we don't re-litigate: the non-generic `IMessageHandler`,
  `IMessageHandlerFactory`, and `IDeferredRequestMapper` are *transitively forced public* by the
  public, cross-assembly-registered (`Benzene.Grpc`/`Benzene.AspNet.Core` do
  `AddScoped<MessageRouter<…>>()`), **DI-constructed** `MessageRouter<TContext>` — its public ctor takes
  `IMessageHandlerFactory`, whose `Create` returns the non-generic `IMessageHandler`, whose `HandleAsync`
  takes `IDeferredRequestMapper`; internalizing any of them would need an architectural change, not a
  visibility flip. `ReflectionMessageHandlersFinder` (used by `Benzene.CloudService`/`CodeGen`) and
  `DefaultResponsePayloadMapper<TContext>` (used by `Benzene.Core.Versioning`) likewise stay public on
  genuine cross-assembly use. Verified: full `Benzene.sln` build green + 1532/1532 core tests pass
  (exercises `MessageHandler<>`/`MessageRouter` construction under **both** MS.DI and Autofac with the
  now-internal types). No `InternalsVisibleTo` additions were needed (`Benzene.Core.MessageHandlers`
  already grants it to `Benzene.Test`). **1.2 complete** — every Decision (A–D) resolved.
- [x] **1.3 Capability-honesty matrix (§6).** Author the single "what Benzene does / deliberately
  doesn't / how to solve it outside" matrix and link it from the README and each affected package.
  Covers idempotency, saga durability, resilience, schema-compat, multi-tenancy, database access,
  async tracing. *Done when: the matrix exists and every capability package's doc points to it.*
  **DONE 2026-07-19:** `docs/capability-matrix.md` exists and is comprehensive — the three-column
  table covers every §6 seed row (transport features, idempotency, resilience, sagas, schema
  evolution, database access, multi-tenancy, distributed tracing) plus message routing, config &
  secrets, outbound HTTP, and auth, and embeds the full per-transport retry-on-handler-failure-result
  breakdown from 3.6. Verified all 12 of its outbound cookbook/doc links resolve. It's linked from the
  README (§ line 29) and `docs/index.md` (line 12). This pass closed the remaining gap — the
  **capability** packages didn't point back to it (only the transport packages did, via 3.6's retry
  table). Added a matrix pointer to all nine capability packages that own a matrix row:
  `Benzene.Idempotency`, `Benzene.Saga` (made its existing prose "§6 capability-honesty matrix"
  reference an actual link), `Benzene.Resilience`, `Benzene.Resilience.Polly`,
  `Benzene.SchemaRegistry.Core`, `Benzene.Diagnostics`, `Benzene.Configuration.Core`,
  `Benzene.Clients.Http`, `Benzene.Auth.OAuth2` — 16 package docs now link the matrix in total. All
  relative links verified to resolve. (Caching has no matrix row today — stampede/single-flight is an
  explicit post-1.0 item — so `Benzene.Cache.*` deliberately doesn't link.)

### Tier 2 — Egress build-out (the real functional gap — §5 has the design)

- [x] **2.1 Segregate AWS clients by transport** — split `Benzene.Clients.Aws` into per-transport
  packages so using the SNS client doesn't drag in SQS. (§5.1) **DONE 2026-07-18:** created
  `Benzene.Clients.Aws.{Sqs,Sns,EventBridge,Lambda,StepFunctions}`, each pinning only its own AWS
  SDK; `Benzene.Clients.Aws` is now a thin meta-package referencing all five. Fixed the Lambda→Sqs
  copy-paste coupling (`nameof(SqsClientMiddleware)` bug). Namespaces unchanged (consumer `using`s
  unaffected); health-check registrations moved onto each transport package's `Extensions`. Verified:
  `Benzene.Clients.Aws.Sns` pulls in `AWSSDK.SimpleNotificationService` only (not `AWSSDK.SQS`); full
  `Benzene.sln` build + 1458-test suite green. Docs (package reference, clients, middleware ref, IAM,
  cookbooks) updated. **Note the `IBenzeneResult<Void>` outbound-cast trap (2.4) still stands** — it
  moved verbatim into `Benzene.Clients.Aws.Sqs`/`.Sns` and is documented there.
- [x] **2.2 Build the Azure egress packages** — the currently-absent outbound half, 1:1 with the
  ingress transports where it makes sense. (§5.2) **DONE 2026-07-18:** created
  `Benzene.Clients.Azure.{ServiceBus,EventHub,EventGrid,QueueStorage}`. Routing properties verified
  against actual ingress code (not assumed): Service Bus/Event Hubs set `"topic"` on
  `ApplicationProperties`/`Properties` (matching `ServiceBusMessageTopicGetter`/
  `EventHubConsumerMessageTopicGetter`); Event Grid sets the CloudEvent `Type`/`EventGridEvent`
  `EventType` (matching `EventGridMessageTopicGetter`, which routes on event type, not
  subject/source); Queue Storage has no properties bag at all, so it serializes a
  `BenzeneMessageRequest` envelope as the body (matching `BenzeneMessageQueueStorageHandler`) — and
  the package docs flag that a `UsePresetTopic(...)` queue needs the raw payload instead, not this
  envelope. Every package takes an already-built SDK client (`ServiceBusSender`/
  `EventHubProducerClient`/`EventGridPublisherClient`/`QueueClient`), mirroring the ingress
  `I*ClientFactory` seam — no `TokenCredential`/connection-string wrapping, consistent with
  principle 1. Event Grid supports both CloudEvents 1.0 (default) and the classic schema, per the
  §5.2 table; a test caught a real bug where CloudEvent `ExtensionAttributes` reject mixed-case
  names (the default `correlationId` header would have thrown at send time) — fixed by lowercasing
  header keys, documented in the package's `CLAUDE.md`. Blob Storage/Cosmos DB Change Feed/Timer
  confirmed **no egress** and that's now stated on each ingress package's `CLAUDE.md` (database/
  storage access and scheduling aren't transports — principle 2). **Kafka decision**: no dedicated
  `Benzene.Clients.Azure.Kafka` — `Benzene.Kafka.Core`'s existing `KafkaBenzeneMessageClient` already
  works unchanged against Event Hubs' Kafka-protocol endpoint; documented in its `CLAUDE.md` rather
  than duplicated as a new package. Verified: full `Benzene.sln` build + `Benzene.Examples.sln` gate
  + 1472-test suite green (14 new converter tests added, one real bug caught and fixed). Docs
  (clients.md, package reference, middleware reference) updated. **Not yet done**: end-to-end
  DI-pipeline tests (only converter-level unit tests exist so far — the AWS egress packages have
  fuller `AddOutboundRouting(...)` integration coverage); that gap plus 2.3/2.4/2.5 remain below.
- [x] **2.3 Demonstrate egress in the examples** — the shared example domain publishes as well as
  consumes, on both clouds, so the ingress↔egress symmetry is copy-runnable. **DONE 2026-07-18:**
  added `OrderCreatedEvent` + the `MessageTopicNames.OrderCreated` topic constant to the shared
  `Benzene.Examples.App` (data-only, no DI coupling — safe for every host that references App, not
  just AWS/Azure). Added a small `PublishOrderCreatedMessageHandler` to each of
  `examples/Aws/Benzene.Examples.Aws` and `examples/Azure/Benzene.Example.Azure` that does nothing
  but call `IBenzeneMessageSender.SendAsync(...)`; each host's `DependenciesBuilder` wires
  `AddOutboundRouting(...)` reusing the SAME queue its own ingress trigger already talks to (AWS:
  the localstack `MY_QUEUE_URL` SQS queue; Azure: the Service Bus `orders` queue), on the distinct
  `order_created` topic — so running either example shows both directions against real
  already-provisioned infra, no new setup required. Deliberately did **not** touch
  `CreateOrderMessageHandler` or any other shared-domain handler — that would have made
  `IBenzeneMessageSender` a mandatory dependency for the 4+ other hosts (Asp/Google/Grpc/Kafka) that
  don't wire egress, for no benefit; the demo handler is reachable exactly like any other handler
  (HTTP) and only resolves `IBenzeneMessageSender` when actually invoked. Verified: `Benzene.sln` +
  `Benzene.Examples.sln` + both per-folder `.sln`s build green; AWS example's existing unit/
  integration test suite unaffected (10/10 pre-existing pass rate unchanged — one pre-existing
  unrelated failure confirmed identical on clean `main`). Docs: `docs/clients.md` (new "Runnable
  example" section) and `examples/CLAUDE.md` updated.
- [x] **2.4 Fix the AWS client `InvalidCastException` trap** — the outbound converters hardcode
  `IBenzeneResult<Void>` and throw at runtime for any other `TResponse`; fix before freeze.
  **DONE 2026-07-18:** the bare cast in `DefaultBenzeneMessageSender.SendAsync` — `(IBenzeneResult<TResponse>)context.Response!` —
  is now a pattern-matched check that throws the new `OutboundResponseTypeMismatchException`
  (`Benzene.Clients`) instead, naming the topic, the actual response type (reflected off the closed
  `IBenzeneResult<T>` the route produced), and the requested `TResponse`. Fixes the trap for every
  send-acknowledgement-only transport at once (SQS, SNS, and the four Tier-2.2 Azure egress
  packages), not just AWS — it's a single fix at the sender, not per-transport. Verified: new tests
  in `DefaultBenzeneMessageSenderTest` (mismatch throws with both types named; matching `Void` still
  succeeds); full `Benzene.sln` build + 1474-test suite green. Docs updated (`Benzene.Clients`,
  `Benzene.Clients.Aws.Sqs`/`.Sns`, `docs/clients.md`).
- [x] **2.5 Step Functions depth** — decide: is fire-and-forget `StartExecution` (currently
  discards the execution ARN) the honest 1.0 scope (document it), or do we add task-token callback?
  Default: scope it honestly for 1.0, deepen post-1.0.
  **DONE 2026-07-19 — honest fire-and-forget scope for 1.0 (task-token deferred post-1.0).** Verified
  from code: `StepFunctionsClient.StartExecutionAsync<TMessage,TResponse>` calls
  `IAmazonStepFunctions.StartExecutionAsync`, returns `BenzeneResult.Accepted<TResponse>()` on success
  (**discarding the `StartExecutionResponse`** — execution ARN + start date not threaded back, and
  `TResponse` carries no value since Step Functions has no synchronous output) and `ServiceUnavailable`
  on a start failure — genuinely fire-and-forget, no await/poll/correlate, no task-token callback.
  The published docs already scope this honestly across the board (`docs/clients.md`,
  `docs/aws-iam-permissions.md` grants only `states:StartExecution`, `docs/cookbooks/sagas.md`,
  `docs/reference/packages.md`, `docs/capability-matrix.md` — all say "*start* an execution, running/
  resuming is Step Functions' job"). Only change needed: the package `CLAUDE.md`'s scope section said
  "decision pending" — updated to record the decision and sharpen the description (ARN discarded,
  `TResponse` empty, use raw `IAmazonStepFunctions` for `DescribeExecution`/task tokens). Task-token
  callbacks / awaited completion remain the explicit Post-1.0 "Durable/orchestration depth" item.

### Tier 3 — Capability honesty & targeted fixes (per the philosophy, mostly docs + small code)

- [x] **3.1 Idempotency — honest scoping.** Keep the abstraction + in-memory (single-process)
  impl. Document plainly that cross-instance dedup is **not** solvable inside Benzene alone
  (independent processes; shared state can just relocate the race — principle 6), that it requires
  an external store with atomic conditional writes (DynamoDB conditional put / Redis SETNX), and
  that handlers should be designed naturally idempotent. Optionally ship a **reference** external
  adapter as a cookbook (not necessarily a package). Also: forward the `CancellationToken` in
  `TryClaimAsync` (small real fix). *Done when: the docs make the runtime limit explicit and show
  the external pattern with its caveats.* **DONE 2026-07-18:** honest-scoping docs were already in
  place from Tier 1. Real fix landed: `InMemoryIdempotencyStore`'s `TryClaimAsync`/`CompleteAsync`/
  `ReleaseAsync` declared `CancellationToken` but never read it — each now calls
  `cancellationToken.ThrowIfCancellationRequested()`. Important scoping discovery, documented rather
  than silently left: `IMiddleware<TContext>.HandleAsync(TContext, Func<Task>)` carries **no**
  `CancellationToken` anywhere across the entire middleware framework (verified against every
  `IMiddleware<TContext>` in the repo) — full end-to-end forwarding would be a breaking change to
  the core interface, out of scope for a "small real fix"; documented as a known constraint in
  `src/Benzene.Idempotency/CLAUDE.md`. The `docs/cookbooks/idempotency.md` reference Redis store was
  also fixed to forward the token via `.WaitAsync(ct)`. New tests added; 22/22 idempotency tests +
  full `Benzene.sln` build green.
- [x] **3.2 Resilience — truth + evaluate a Polly integration.** (a) Fix the docs to "retry with
  exponential backoff only" and drop every Polly/CB/timeout/bulkhead claim (part of 1.1). (b)
  **Evaluate `Benzene.Resilience.Polly`** — a thin package that lets a user drop their own Polly
  `ResiliencePipeline` into Benzene middleware, *exposing* Polly rather than wrapping it (principle
  4). If it earns its keep, build it; if the plumbing is trivial enough to be a cookbook, document
  it instead. Add jitter + a max-delay cap to the existing `RetryMiddleware` and document that
  retry re-invokes the downstream pipeline (don't wire it on an inbound HTTP context that already
  wrote a response). **DONE 2026-07-18:** (a) was already fixed in Tier 1. (b) decided **cookbook,
  not package** — the Polly bridge is ~15 lines (`PollyMiddleware<TContext>` wrapping
  `ResiliencePipeline.ExecuteAsync`) with zero Benzene-specific plumbing, well under the bar for a
  new frozen-at-1.0 NuGet surface + transitive Polly dependency; documented in
  `docs/cookbooks/polly-resilience.md` (includes the outbound-client usage, a cancellation caveat —
  Benzene's pipeline threads no `CancellationToken`, per 3.1's finding — and a testing example).
  `RetryMiddleware<TContext>` gained `maxDelay` (`TimeSpan?`, caps the sleep only — the underlying
  exponential growth stays uncapped so later attempts still compound correctly) and `jitter`
  (`Func<TimeSpan,TimeSpan>`, default no-op) constructor/`.UseRetry(...)` parameters, plus a
  ready-made `RetryMiddleware.FullJitter()` static helper (AWS's "full jitter" algorithm) on a new
  non-generic `RetryMiddleware` companion class (avoids forcing callers to write
  `RetryMiddleware<SomeArbitraryType>.FullJitter()`). Fully additive — both new parameters default
  to the prior exact behavior, verified by the pre-existing exact-backoff test passing unmodified.
  4 new tests added (13/13 Resilience tests green); full `Benzene.sln` build + 1480-test suite
  green. Docs updated: package `CLAUDE.md`, `docs/resilience.md` (config table, new "capping delay
  and jitter" example, troubleshooting note), `docs/capability-matrix.md`, `docs/cookbooks/README.md`.
- [x] **3.3 Schema-compat — honest scoping.** Document that the shipped `TextualSchemaCompatibilityChecker`
  only accepts byte-identical schemas (the enum modes exist but structural Avro/JSON evolution needs
  a user-supplied checker or a real registry server). Keep the solid `ConfluentWireFormat` codec as
  the showcase. *Done when: nobody expects structural compat-checking from the in-box checker.*
  **DONE 2026-07-18:** verified from code — `TextualSchemaCompatibilityChecker.IsCompatible` is
  exactly an ordinal string-equality check; `Backward`/`Forward`/`Full` modes are all accepted as
  config but collapse to that same identity check (none is differentiated in the shipped checker).
  `ConfluentWireFormat` confirmed as a correct, independent 5-byte wire-format codec (magic byte +
  schema ID), genuinely showcase-worthy and unaffected by the compat-checker's limits. This was
  already correctly documented from Tier 1 (`src/Benzene.SchemaRegistry.Core/CLAUDE.md`,
  `docs/cookbooks/schema-registry.md`, `docs/capability-matrix.md`); the only gap found was
  `docs/cookbooks/README.md`'s one-line index summary implying more than byte-identical gating —
  reworded.
- [x] **3.4 Saga — honest scoping.** Document that sagas are in-process compensation flows with
  **no durable crash-resume** (the state store is for observability, not recovery); a crashed saga
  needs manual reconciliation. State it as a deliberate boundary; point at durable orchestration
  (Step Functions / a workflow engine) for teams that need crash-durable long-running workflows.
  **DONE 2026-07-18:** verified from code — the saga engine's `ISagaStateStore` calls
  (`RecordStartedAsync`/`RecordStageCompletedAsync`/`RecordFinishedAsync`) are write-only; nothing
  anywhere reads the store back to resume/rehydrate/replay a saga, and steps are in-memory `Func`
  closures that can't be serialized regardless. Confirms the plan's own summary was accurate, not
  just trusted. This was already correctly documented from Tier 1
  (`src/Benzene.Saga/CLAUDE.md`, `docs/cookbooks/sagas.md`, `docs/capability-matrix.md`); tightened
  the capability-matrix "solve it via" cell to also name Temporal and clarify that
  `Benzene.Clients.Aws.StepFunctions` can *start* an execution but doesn't run/resume the workflow,
  and added an explicit "crash-durable orchestration is out of scope" bullet to the sagas cookbook.
- [x] **3.5 Async-transport tracing** — the highest-value *real* observability fix: implement W3C
  `traceparent` inbound extraction for SQS/SNS/Kafka/Event Hub (currently HTTP-only) and stop
  dropping `invocationId` in per-message sub-pipelines. Decide whether this lands in 1.0 (it's the
  differentiating transport set) or is a fast-follow with an honest "HTTP-only today" note.
  **DONE 2026-07-18 — lands in 1.0.** Investigation up front changed the shape of this work
  substantially: `Benzene.Diagnostics.UseW3CTraceContext<TContext>()` (the inbound extraction
  middleware) was **already fully generic and transport-agnostic** — the "HTTP-only" framing
  described that nobody had wired it into an async transport's pipeline anywhere in the codebase
  yet, not a code limitation. Most transports' `IMessageHeadersGetter<TContext>` already read real
  headers correctly. Two genuine gaps were found and fixed (not just documented):
  `Benzene.Azure.Function.Kafka`'s header getter was a stub always returning `{}` — confirmed via
  reflection against the installed `Microsoft.Azure.Functions.Worker.Extensions.Kafka` 4.3.0 that
  the bound type genuinely does expose headers, so this was a Benzene oversight, not a platform
  limitation — now reads them for real; `Benzene.Azure.Function.EventHub`'s `EventHubContext` had
  **no** `IMessageHeadersGetter<EventHubContext>` registered at all (the package had no first-class
  mappers, only the envelope-routing path) — added one reading `EventData.Properties`. `.UseW3CTraceContext<TContext>()`
  is now proven end-to-end (new tests, following the existing `W3CTraceContextTest.cs` harness) on
  all eight transport/host combinations: SQS (AWS Lambda + self-hosted worker), SNS (AWS Lambda),
  Kafka (AWS Lambda, the self-hosted `Benzene.Kafka.Core` worker, Azure Functions trigger), and
  Event Hub (Azure Functions trigger, self-hosted `Benzene.Azure.EventHub` worker).
  <br><br>
  **The `invocationId` fix** was a real, separate bug, root-caused precisely: every batch/per-message
  transport dispatches each record through its own fresh DI scope
  (`serviceResolverFactory.CreateScope()`, via `MiddlewareMultiApplication` or the transport's own
  per-record loop), and `IBenzeneInvocation` is DI-scoped, not ambient — a fresh scope's accessor was
  always `null`, so `UseBenzeneEnrichment`'s `invocationId` log-scope key was silently omitted
  (`TryGetService` swallowed the `BenzeneException` the factory throws when unpopulated). Fixed by
  adding a `UseBenzeneInvocation()` convenience overload per per-message context type — deriving
  `InvocationId` from that message's own natural identifier (SQS/SNS `MessageId`; Kafka
  `"{topic}-{partition}-{offset}"`, since Kafka has no single message-id field; Event Hub's
  service-assigned `SequenceNumber`) — and auto-wiring it as the first middleware inside each
  transport's own `UseSqs`/`UseSns`/`UseKafka`/`UseEventHub` pipeline composition (both AWS Lambda
  and Azure Functions triggers, plus the self-hosted `Benzene.Aws.Sqs`/`Benzene.Kafka.Core`/
  `Benzene.Azure.EventHub` workers — eight call sites total). **No application code changes are
  required** — this is a transparent, purely-additive fix, not a new opt-in feature; verified by the
  pre-existing test suite passing unmodified plus new regression coverage reproducing the exact
  nested-scope bug shape (populate `IBenzeneInvocation` in an outer scope, assert a fresh inner scope
  resolves its own value rather than throwing or coming back stale/null).
  <br><br>
  Verified: full `Benzene.sln` + `Benzene.Examples.sln` build green; 1498-test suite green (stable
  across repeated runs — checked for the test-parallelization flakiness one contributing agent
  flagged, not reproduced at full-suite scale), up from 1474 before this item (24 new tests: 8
  `UseBenzeneInvocation()` unit tests + 1 nested-scope regression test + getter/W3C-trace-context
  tests for the two newly-fixed Azure Function packages + the pre-existing test suite unaffected).
  Docs updated: `Benzene.Diagnostics/CLAUDE.md`, `docs/monitoring.md`, `docs/capability-matrix.md`,
  `docs/cookbooks/distributed-tracing-opentelemetry.md` (the worker-side example now actually calls
  `.UseW3CTraceContext()` and the cookbook's "current limitation" section became a "this now works"
  walkthrough), and every touched transport package's own `CLAUDE.md`.
- [x] **3.6 SNS drop-on-failure default** — reconsider `RaiseOnFailureStatus = false` default (a
  handler returning a failure result is silently accepted / message lost); at minimum document it
  prominently. Same review for any transport with an unsafe-by-default reliability posture.
  **DONE 2026-07-18:** confirmed from code — with the `false` default, a handler returning a
  non-exception failure result never throws, so `SnsApplication.HandleAsync` reports success to
  Lambda regardless; SNS never retries and there's no DLQ (a thrown exception, by contrast, already
  propagates safely today). Added a prominent top-of-file warning to `Benzene.Aws.Lambda.Sns`'s
  `CLAUDE.md` plus supporting notes in the SNS fan-out cookbook, `docs/message-result.md`, and
  `docs/getting-started-aws.md`. **Full cross-transport review** (the plan's "same review for any
  transport" ask) added a new "Retry-on-handler-failure-result" table to
  `docs/capability-matrix.md` covering every AWS Lambda/self-hosted-worker and Azure Function/
  self-hosted-worker transport: SQS (Lambda `PartialBatchFailure`), DynamoDB Streams, and Kinesis
  confirmed safe-by-default; Azure Service Bus (both Function + worker) and Azure Kafka/Event Hubs
  Function trigger, plus the self-hosted `Benzene.Aws.Sqs` `SqsConsumer`, confirmed unsafe-by-default
  but configurable (documented, top-of-file warnings added); **AWS Lambda EventBridge/Kafka/S3 and
  Azure Event Grid/Queue Storage/Event Hubs-worker confirmed unsafe with no opt-out at all** (worse
  than SNS — no knob exists to escalate a failure result into a retry), warnings added to each.
  Bonus fix: `Benzene.Aws.Lambda.Kafka`'s `CLAUDE.md` had a false claim ("failed records can trigger
  batch reprocessing" implying per-record retry) — corrected to state only a thrown exception
  replays the whole batch (no `ReportBatchItemFailures` implementation exists). All changes
  doc-only; no default values changed (a maintainer call, left to the maintainer).

### Tier 4 — Consistency & polish (credibility papercuts for a public launch)

- [x] **4.1 Fix the `Kakfa` typo** across the Kafka example project/namespace/solution + the two
  docs that reference it (mechanical, multi-file). **DONE 2026-07-19 (maintainer-approved):** renamed
  the two misspelled projects `Benzene.Examples.Kakfa` → `Benzene.Examples.Kafka` and
  `Benzene.Examples.Kakfa.Producer` → `Benzene.Examples.Kafka.Producer` (directories + `.csproj`
  files via `git mv`), and replaced every `Kakfa` occurrence in buildable source: the two `.sln`
  files (`Benzene.Examples.sln` + `examples/Kafka/Benzene.Example.Kafka.sln`), the namespaces/usings
  (`DependenciesBuilder.cs`/`Program.cs`/`StartUp.cs`/`KafkaSender.cs`), the `UserSecretsId` +
  `launchSettings.json`, the Kafka test project's `ProjectReference`s + `WorkerSetUp.cs` using, and the
  two docs (`getting-started-kafka.md`, `getting-started-worker.md`). Verified: both renamed projects
  + the Kafka test project build green, `Benzene.Examples.sln` lists the renamed projects, and the
  inner `Benzene.Example.Kafka.sln` builds. Historical mentions in `work/*` and `CHANGELOG.md` left
  as-is (records of the past); the `examples/CLAUDE.md` "known quirks" note updated to reflect the
  fix. Pre-existing dangling reference noted (not introduced here): `Benzene.Examples.Kafka.Dev.Test`
  references a `*.Dev.csproj` that doesn't exist and isn't in `Benzene.Examples.sln`.
- [ ] **4.2 Reconcile `Benzene.Example.*` vs `.Examples.*`** singular/plural split (bundle with 4.1
  if touched). **DEFERRED — maintainer decision (2026-07-19).** Purely-cosmetic repo-wide rename across
  ~6 example folders and every `.sln` (`Benzene.Example.Asp/Azure/Cloudflare/Grpc/Kafka.sln` singular
  vs `Benzene.Examples.Aws.sln` plural, plus the project names inside), zero functional benefit; the
  checked-in `examples/CLAUDE.md` explicitly says leave-unless-asked. Maintainer chose to do 4.1 only
  and hold 4.2 — left open pending an explicit decision on the target convention.
- [x] **4.3 README quickstart ↔ getting-started parity** — the README handler snippet is missing
  the load-bearing `[HttpEndpoint]` and uses a different type name than the runnable getting-started;
  make the README a strict subset (or mark it not-runnable-as-is).
  **DONE 2026-07-19:** made the README handler a strict subset of the canonical `docs/getting-started.md`
  handler — added the load-bearing `[HttpEndpoint("GET", "/hello/{name}")]` (without it the ASP.NET
  host wired below with `.UseHttp(...UseMessageHandlers())` routes nothing — getting-started states the
  middleware "only responds to requests that match one of your `[HttpEndpoint]` routes") and renamed the
  request type `HelloWorldMessage` → `HelloWorldRequest` to match. The README's host-wiring block already
  matched getting-started verbatim; the request/response type definitions stay intentionally omitted
  ("omitted above for brevity") and now name the same types the walkthrough defines.
- [x] **4.4 Out-of-scope surfaces** — mark Google/Cloudflare examples & docs "experimental /
  community" (or hold them out of the 1.0 cut) so they don't read as half-finished supported work.
  **DONE 2026-07-19:** added a consistent "⚠️ Experimental / community-supported — not part of the
  Benzene 1.0 support commitment … out of scope for the 1.0 release" banner to the top of every
  user-facing Google/Cloudflare surface — `docs/getting-started-cloudflare.md`,
  `examples/Cloudflare/README.md`, `examples/Google/README.md` — and tagged the Cloudflare entry in
  the `docs/index.md` nav *(experimental / community — out of scope for 1.0)*. (Google has no
  getting-started page and isn't in the nav; `docs/reference/packages.md` lists neither, so no
  further surfaces needed labelling.)
- [x] **4.5 AWS HTTP API v2** — either add an `APIGatewayHttpApiV2ProxyRequest` context/adapter, or
  scope every doc claim to "REST API + HTTP API payload format 1.0" and say so loudly (ties to 1.1).
  **DONE 2026-07-19 — documentation path (v2 adapter deferred post-1.0).** Verified the scope is
  already stated loudly and no doc overclaims v2: `Benzene.Aws.Lambda.ApiGateway/CLAUDE.md` has a
  dedicated "**Payload format version 1.0 only**" section ("Only `APIGatewayProxyRequest` is wrapped
  — this covers REST APIs and HTTP APIs configured for payload format 1.0. There is deliberately no
  v2 support; `APIGatewayHttpApiV2ProxyRequest` is not referenced anywhere in the package"), and a
  repo-wide grep of `docs/`+`README.md` finds **no** "HTTP API v2 / payload format 2 /
  APIGatewayHttpApiV2" claim. The 1.1 doc-truth audit independently confirmed the same. A real v2
  context/adapter was then treated as a post-1.0 enhancement — **and has since been built**: the full
  `ApiGatewayV2*` adapter set now ships in `src/Benzene.Aws.Lambda.ApiGateway/`, alongside the ALB
  bridge (`Benzene.Aws.Lambda.AspNet`/`Benzene.Aws.Lambda.HttpBridge`), so the "v1 only" scope note
  above is history rather than the current shape.
- [x] **4.6 API Gateway request fidelity** — query-string params and multi-value headers are dropped
  in the adapter; `IsBase64Encoded` bodies aren't decoded. Fix or document the limitation.
  **DONE 2026-07-19 — fixed the real bug, documented the contract-level limits.** Investigation
  corrected the premise: single-value query-string **and** path params *are* already surfaced onto the
  request object by `ApiGatewayRequestEnricher` (reads `QueryStringParameters`/`PathParameters`/
  `Headers`) — so "query strings dropped" was imprecise; what's actually dropped is the **multi-value**
  forms (`MultiValueHeaders`/`MultiValueQueryStringParameters`), because the getter/enricher contracts
  are single-value `IDictionary<string,string>`. **Fixed** the clear correctness bug:
  `ApiGatewayMessageBodyGetter` now base64-decodes the body (UTF-8) when `IsBase64Encoded` is set instead
  of handing the handler raw base64 (binary media types / non-text payloads were broken); regression test
  added (`ApiGatewayMessageBodyGetterTest.Map_Base64EncodedBody_IsDecoded`, both getter tests green,
  package builds). **Documented** the multi-value single-value-only limitation precisely in the package
  `CLAUDE.md` (last-value-wins, full multi-value threading deferred post-1.0) and refreshed the now-fixed
  base64 bullet.

### Tier 5 — Examples testing initiative (closing the pre-1.0 confidence gap — `work/examples-testing-plan.md` has the full design)

- [~] **5.1 In-memory tier for every 1.0-scoped example** — *mostly done 2026-07-19.* New
  `Benzene.Testing`-based test projects added for App, Azure, Saga, and OpenTelemetry (+ an egress
  test filling a gap in the existing Aws suite); `build-benzene.yml`'s `examples-build` job now
  **runs** the seven in-memory example test projects (App/Asp/Aws/Azure/OpenTelemetry/Saga/Google,
  ~103 tests), turning examples from compile-only into a real regression gate. Found/fixed three
  real bugs along the way (missing `.AddBenzene()` in the Azure example; `PublishOrderCreatedMessageHandler`
  undiscoverable in both Aws+Azure examples; a stale wrong-path test). Remaining: **Grpc** (needs a
  host-build shim — deferred). AwsMesh/AzureMesh/Mesh dropped from in-memory scope (not in the
  solution / inherently live-integration — see `work/examples-testing-plan.md`).
- [~] **5.2 AWS LocalStack tier, spike-then-decide** — *SQS egress proof shipped 2026-07-19.*
  `examples/Aws/Benzene.Examples.Aws.Dev.Test` runs the AWS example's egress demo end-to-end against
  a real LocalStack SQS queue (real `.UseSqs` route, drained + asserted with the AWS SDK), with its
  own `examples-aws-localstack-tests` CI job. Compile-verified locally; runtime-verified in CI (no
  local Docker daemon here). The Lambda-into-LocalStack spike is **deferred** pending a live Docker
  loop / a green CI baseline — full rationale + intended approach in `work/examples-testing-plan.md`.
- [ ] **5.3 Extend the real-dependency tier to Azure/Kafka** — a test project against the already
  docker-composed-but-untested Service Bus/Event Hub/Azurite emulators in
  `examples/Azure/Benzene.Example.Azure.Worker`, and confirm `Benzene.Examples.Kafka.Dev.Test` is
  actually CI-wired.
- [ ] **5.4 Cross-cutting concerns coverage matrix** — map idempotency/retry/W3C-trace/health/
  validation/versioning/sagas/egress-ingress-symmetry against which example's tests actually prove
  each one out end-to-end, and close gaps.

### Post-1.0 (genuine roadmap, explicitly deferred — must be *stated*, not silent)

- Azure Service Bus **request/reply** (`ReplyTo`/`CorrelationId`). *(Sessions / ordered-per-key
  processing have since shipped — see "Delivered since this section was written" below.)*
- Durable/orchestration depth (Step Functions task-token callbacks; an Azure orchestration story).
- Circuit breaker / timeout / bulkhead (via 3.2's Polly integration if that's the chosen path).
- Shipped cloud-secret adapters (`Benzene.Configuration.Azure`/`.Aws`) — today they're cookbook
  snippets (parity across clouds, so a considered choice, but enterprise-friction).
- Caching stampede/single-flight protection; `IHttpClientFactory`-based outbound HTTP guidance.
- Durable idempotency/saga reference adapters (if not shipped as cookbooks in Tier 3).
- Performance / cold-start benchmarks (needs real deployed cloud resources — can't be done in the
  current sandbox).
- AWS SDK v4 migration decision; FIFO/dedup/batch-send on the SQS/SNS clients; Cognito/JWT
  authorizer helpers.
- AuthZ policy-engine (OPA/Cedar) adapter; multi-tenancy support (absent today).

**Delivered since this section was written** (verified against source 2026-08-20 — kept listed so the
section is not read as still-deferred):
- **ALB / Lambda Function URL adapters** — `src/Benzene.Aws.Lambda.AspNet/BenzeneAspNetAlbBridge.cs`
  and `src/Benzene.Aws.Lambda.HttpBridge/`.
- **API Gateway HTTP API payload format 2.0** — the full `ApiGatewayV2*` adapter set (application,
  context, request/response adapters, getters, enricher, handler) in
  `src/Benzene.Aws.Lambda.ApiGateway/`.
- **Azure Service Bus sessions / ordered-per-key processing** — `BenzeneServiceBusConfig.SessionsEnabled`
  / `MaxConcurrentSessions` / `MaxConcurrentCallsPerSession`, driven by `BenzeneServiceBusWorker`'s
  session processor. (Service Bus **request/reply** is still deferred.)
- **Rate-limiting middleware** — `src/Benzene.RateLimiting/RateLimitingMiddleware.cs`; the
  `TooManyRequests` status now has a producer.

---

## 5. Egress design (Tier 2 detail)

The organising rule (principle 2): **every messaging *transport* gets an egress package that mirrors
its ingress; things that aren't transports (databases, stores, schedulers) get no egress — you bring
your own code.**

### 5.1 AWS client segregation

Today all AWS outbound clients live in one package, `src/Benzene.Clients.Aws` (SQS, SNS,
EventBridge, Lambda-invoke, StepFunctions). Using the SNS client therefore drags in the SQS client
and its SDK. Split along the existing folder boundaries into per-transport packages, mirroring how
ingress is split (`Benzene.Aws.Lambda.Sqs`, `.Sns`, …):

| New package (proposed) | Contains (from today's `Benzene.Clients.Aws/…`) |
|---|---|
| `Benzene.Clients.Aws.Sqs` | `Sqs/*` — SQS send client + outbound converter + health check |
| `Benzene.Clients.Aws.Sns` | `Sns/*` — SNS publish client + converters |
| `Benzene.Clients.Aws.EventBridge` | `EventBridge/*` — PutEvents client |
| `Benzene.Clients.Aws.Lambda` | `Lambda/*` — Lambda invoke client |
| `Benzene.Clients.Aws.StepFunctions` | `StepFunctions/*` — StartExecution client (scope per 2.5) |
| `Benzene.Clients.Aws.Core` *(if needed)* | any shared outbound wiring currently in `Clients.Aws/Extensions.cs` |

Shared transport-agnostic outbound infrastructure (routing, correlation, trace-context propagation)
stays in `Benzene.Clients` (the existing base — the outbound analogue of `Benzene.Aws.Lambda.Core`).
`Benzene.Clients.Aws` becomes either a thin meta-package that references all five (for people who
genuinely want everything) or is retired — **naming/meta-package call for the maintainer.** Each new
package pins only the one AWS SDK it needs. *Done when: referencing `Benzene.Clients.Aws.Sns` pulls
in `AWSSDK.SimpleNotificationService` only, not `AWSSDK.SQS`.*

### 5.2 Azure egress packages (currently absent entirely)

Build the outbound half so an Azure Benzene service can publish with the same correlation/trace
propagation the AWS path gets for free. 1:1 with ingress **where the target is a transport**:

| Azure transport | Ingress (exists) | Egress (build) | Notes |
|---|---|---|---|
| Service Bus | `Benzene.Azure.Function.ServiceBus` + `Benzene.Azure.ServiceBus` | **`Benzene.Clients.Azure.ServiceBus`** | `ServiceBusSender` to queue/topic; set the `topic` app-property Benzene routes on |
| Event Hubs | `Benzene.Azure.Function.EventHub` + `Benzene.Azure.EventHub` | **`Benzene.Clients.Azure.EventHub`** | `EventHubProducerClient` |
| Event Grid | `Benzene.Azure.Function.EventGrid` | **`Benzene.Clients.Azure.EventGrid`** | `EventGridPublisherClient`; support both EG-schema and CloudEvents |
| Queue Storage | `Benzene.Azure.Function.QueueStorage` | **`Benzene.Clients.Azure.QueueStorage`** | `QueueClient.SendMessage` (Benzene envelope body) |
| Kafka (over Event Hubs) | `Benzene.Azure.Function.Kafka` | **`Benzene.Clients.Azure.Kafka`** *(or fold into EventHub)* | producer over the Kafka endpoint — decide whether it's its own package or covered by the Event Hub producer |
| HTTP | `Benzene.Azure.Function.AspNet` | *(cross-cloud)* `Benzene.Clients.Http` | already exists; not Azure-specific |
| **Blob Storage** | `Benzene.Azure.Function.BlobStorage` | **none** | writing a blob is *storage access*, not a transport — bring your own `BlobClient` (principle 2) |
| **Cosmos Change Feed** | `Benzene.Azure.Function.CosmosDb` + worker | **none** | Cosmos is a **database, not a transport**. No egress. Persisting to Cosmos is your handler's own `CosmosClient` code (principle 2) |
| **Timer** | `Benzene.Azure.Function.Timer` | **none** | a scheduler trigger — nothing to publish |

Shared transport-agnostic outbound infra stays in `Benzene.Clients`; a `Benzene.Clients.Azure.Core`
holds any Azure-shared wiring / the `TokenCredential` managed-identity seam (consistent with the
ingress factories). Each package pins only its one Azure SDK. *Done when: an Azure handler can
publish to Service Bus/Event Hubs/Event Grid/Queue Storage through Benzene with correlation + trace
propagation, and the "no egress for Blob/Cosmos/Timer — bring your own code" rule is documented on
each of those ingress packages.*

### 5.3 The symmetry principle, documented

Publish an **ingress↔egress matrix** (both clouds) as a first-class doc page, with the deliberate
asymmetries (Blob/Cosmos/Timer/S3-notifications have no egress because they aren't transports)
stated as *design*, not omission. This doc is itself a strong expression of the philosophy and a
good adoption asset.

---

## 6. Capability-honesty matrix (Tier 1.3 — the deliverable shape)

A single table (README-linked, and linked from each capability package) with three columns:
**What Benzene does · What it deliberately doesn't (and why) · How to solve it outside Benzene.**
Seed rows:

| Capability | Benzene provides | Deliberately NOT (why) | Solve it via |
|---|---|---|---|
| Idempotency | Seam + in-memory single-process store; atomic-claim middleware | Cross-instance dedup — independent processes can't coordinate at runtime without external state, and shared state can just relocate the race (principle 6) | External store with atomic conditional write (DynamoDB conditional put / Redis SETNX) + naturally-idempotent handlers; reference cookbook |
| Resilience | Retry w/ exponential backoff middleware | Circuit breaker / timeout / bulkhead — best served by Polly, which we won't hide (principle 4) | Your own Polly `ResiliencePipeline` in middleware (± a thin `Benzene.Resilience.Polly` pipe — Tier 3.2) |
| Saga / workflow | In-process compensation-based saga with LIFO rollback | Durable crash-resume (closures can't be rehydrated) | Step Functions / a durable workflow engine for crash-durable orchestration |
| Schema evolution | Confluent wire-format codec; registry seam | Structural Avro/JSON backward-compat checking in-box (checker is byte-identical only) | A real schema-registry server, or supply your own `ISchemaCompatibilityChecker` |
| Database access | *(nothing — by design)* | Any DB/store abstraction (a DB isn't a transport — principle 2) | Your handler's own SDK/ORM (`CosmosClient`, EF Core, Dapper, …) |
| Transport features | Full access to the underlying SDK/message | Hiding transport-specific capabilities behind a generic interface (the Dapr anti-pattern — principle 1) | Use the raw SDK feature directly; the context exposes the native message |
| Multi-tenancy | *(nothing today)* | — (candidate post-1.0) | Roll a tenant-resolver middleware + scoped holder (documented pattern) |
| Distributed tracing | W3C trace context on HTTP transports; OTel exporter-agnostic | Async-transport trace extraction today (Tier 3.5 may close this) | Until 3.5 lands: propagate correlation via message headers |

---

## 7. Appendix — findings by lens (evidence)

The five raw assessments (2026-07-18), each verified against code, are the evidence base for §3–§6.
Rather than duplicate them here, the load-bearing specifics are folded into the tiered worklist with
their `file:line` citations. Headline per lens:

- **AWS:** code more mature than its docs; blockers are the doc-truth pass and the HTTP-API-v2
  claim; strong: SQS partial-batch-failure, DynamoDB/Kinesis streaming. Clients need segregation
  (§5.1) and the `InvalidCastException` fix.
- **Azure:** inbound trigger matrix comprehensive and showcase-worthy; the defining gap is **no
  egress** (§5.2); scope SB sessions/request-reply, Durable, Cosmos-as-store as non-goals.
- **ASP.NET/self-hosted/infra:** worker/k8s story is a genuine strength; `Benzene.Resilience` doc
  oversell is the top blocker; ASP.NET is "hexagonal endpoints coexisting with ASP.NET middleware,"
  not minimal-API parity — say so.
- **Core:** the pipeline/versioning/results/authN are ready to freeze; capability packages are
  abstraction+in-memory-only; the API-freeze surface/naming decisions are the expensive-to-get-wrong
  items; `work/` docs are systematically stale.
- **OSS-release:** license contradiction, repo-root junk, and the `-alpha`-hardcoded pipeline are
  hard blockers to the "1.0" claim; onboarding docs (`getting-started.md`) are excellent and should
  be kept as-is.

---

## 8. How we work this plan

- Iterate tier by tier; within a tier, order by the done-when that unblocks the most.
- Every closed checkbox is verified against code (build/test/read), never against a doc.
- Merge each unit of work to `main` behind a green build + the (new) examples gate.
- Keep this document current — it is the single source of truth; the older `work/*roadmap*` and
  `*checklist*` docs are history and must not be cited as status.
- Strategic calls that are the maintainer's (package-tier meta-naming, the naming-wart freeze,
  whether async-tracing and the Polly package land in 1.0 vs. fast-follow) are flagged inline — we
  decide them as we reach them, not silently.
