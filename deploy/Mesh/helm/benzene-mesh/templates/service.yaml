apiVersion: v1
kind: Service
metadata:
  name: {{ include "benzene-mesh.fullname" . }}
  labels:
    {{- include "benzene-mesh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "benzene-mesh.selectorLabels" . | nindent 4 }}
