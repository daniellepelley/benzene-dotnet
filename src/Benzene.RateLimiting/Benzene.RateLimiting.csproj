<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

	<ItemGroup>
		<None Include="docs\README.md" Pack="true" PackagePath="\" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="System.Threading.RateLimiting" Version="10.0.0" />
	</ItemGroup>

	<ItemGroup>
		<!-- Lets RateLimitingPipelineTest resolve the internal InternallyOwnedRateLimiterHolder<TContext>
		     (#200) directly, the same way the container itself does, rather than only observing its
		     effects indirectly. -->
		<InternalsVisibleTo Include="Benzene.Test" />
	</ItemGroup>

	<ItemGroup>
	  <ProjectReference Include="..\Benzene.Abstractions.Pipelines\Benzene.Abstractions.Pipelines.csproj" />
	  <ProjectReference Include="..\Benzene.Core.MessageHandlers\Benzene.Core.MessageHandlers.csproj" />
	  <ProjectReference Include="..\Benzene.Core.Middleware\Benzene.Core.Middleware.csproj" />
	</ItemGroup>

</Project>
